---
# Compatibility entrypoint.  Atomic release cutover is now the only supported
# deploy mechanism; leaving the old live-path playbook callable would let a
# manual run mutate the read-only published release.
- name: import the atomic release deploy playbook
  ansible.builtin.import_playbook: deploy_atomic.yml
