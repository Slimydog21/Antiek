{
  "agreement": "8/10",
  "summary": "Directionally sound, not precise (rigor #1). The primary radon-failure guard holds — dedup.py (212/335) and errors.py (320/335) rank deep, so the ratio did NOT collapse into raw cyclomatic complexity. 8 of 10 hand-judged UNFENCED modules agree with a careful read. The 2 disagreements share one root cause: rank_score = ratio x log1p(churn) foregrounds shallow-AND-hot, so two HOT but only median-shallow modules (schemas/events.py churn 17, graph/search.py churn 8) sit near the head on churn, not shallowness. Treat the head as a candidate list weighted by both axes; SPR-04 reads `ratio` (shallowness) alongside `rank_score` (priority). Two INDEPENDENT verifiers corroborate the core (see independent_corroboration).",
  "sample": "5 top-ranked UNFENCED (alleged shallow) + 5 bottom-ranked UNFENCED (alleged deep), out of 335 modules; refreshed after the fence-prefix fix",
  "fence_fix_note": "An earlier draft's MASTER_FENCE_PREFIXES used legal/ ad/ ads/ marketplace/ which match ZERO substrate dirs (real dirs: legal_gate/ ad_inventory/ ad_targeting/ advertisers/ marketplace_metrics/). The SPR-01 verifier caught this; fixed + a build_report self-check now records unmatched_master_prefixes (now empty). The fix correctly fenced marketplace_metrics/from_conn.py, ad_inventory/__main__.py, legal_gate/audit.py (previously in this sample); replaced here with unfenced modules. Fence is orthogonal to ratio, so no prior ratio judgment changed.",
  "table": [
    {"module": "substrate/graph/ops.py", "alleged": "shallow", "proxy_rank": "39/335", "verdict": "agree", "note": "14 insert_* funcs each with a wide param list = genuinely wide interface over per-fn DB logic; fair SPR-04 target (top_unfenced[0])"},
    {"module": "substrate/schemas/events.py", "alleged": "shallow", "proxy_rank": "40/335", "verdict": "disagree", "note": "ratio 0.143 = MEDIAN (flat 0.143 too), not shallow; ranks #2 only because it is the hottest file (churn 17). Hot, not shallow — the churn-conflation limitation"},
    {"module": "substrate/graph/search.py", "alleged": "shallow", "proxy_rank": "44/335", "verdict": "disagree", "note": "ratio 0.154 = median; a real vector-search module (Protocol + cosine + search) over 107 depth. Ranks high on churn 8, not shallowness"},
    {"module": "substrate/books/ingest.py", "alleged": "shallow", "proxy_rank": "45/335", "verdict": "agree", "note": "one register_book() with a wide (~11-param) signature over a modest body — a mildly shallow wide-interface function"},
    {"module": "substrate/contracts/reading_surface.py", "alleged": "shallow", "proxy_rank": "47/335", "verdict": "agree", "note": "provisional Protocol stub — 3 methods over ~zero implementation. Genuinely shallow (independent verifier concurs)"},
    {"module": "substrate/research_artifact/render.py", "alleged": "deep", "proxy_rank": "293/335", "verdict": "agree", "note": "one public render_html() over 142 lines of HTML assembly — small interface, real body"},
    {"module": "substrate/synthesis_rubric/scorer.py", "alleged": "deep", "proxy_rank": "296/335", "verdict": "agree", "note": "score_synthesis + CompositeRubricScore over 5 private scoring dimensions (the craft-signature module) — small interface, substantial implementation"},
    {"module": "substrate/cli/__main__.py", "alleged": "deep", "proxy_rank": "298/335", "verdict": "agree", "note": "one public main() dispatcher over the subcommand table"},
    {"module": "substrate/cli/compact.py", "alleged": "deep", "proxy_rank": "299/335", "verdict": "agree", "note": "single command entry over 110 depth — textbook deep"},
    {"module": "substrate/cli/hooks.py", "alleged": "deep", "proxy_rank": "300/335", "verdict": "agree", "note": "single command entry over 123 depth — deep"}
  ],
  "anchors": [
    {"module": "substrate/dedup.py", "expected": "deep/low", "actual_rank": "212/335", "result": "PASS — deep, the primary radon-failure guard"},
    {"module": "substrate/errors.py", "expected": "deep/low", "actual_rank": "320/335", "result": "PASS — deep"},
    {"module": "substrate/research_bridge/db_path.py", "expected": "comparatively shallow", "actual_rank": "ratio 0.167 > dedup 0.088", "result": "PASS on ratio (fenced, so out of top_unfenced)"},
    {"module": "substrate/results.py", "expected": "deep/low", "actual_rank": "63/335 (ratio 0.353, top quartile)", "result": "DIVERGENCE — documented, not gamed"}
  ],
  "anchor_divergences": [
    "results.py: the spec pins it as a deep anchor, but the proxy ranks it moderately shallow (ratio 0.353). By Ousterhout's literal test — interface much simpler than implementation — results.py is a THIN-but-valuable wrapper: Ok/Err expose ~16 one-line methods (map/and_then/unwrap...) over trivial pydantic-delegated bodies. Its depth is SEMANTIC (the error-channel discipline), invisible to a syntactic AST proxy. It stays top-quartile under BOTH weighted (0.353) and flat/unweighted (0.603) surface, so the placement is robust, not a weight artifact. Forcing it deeper would overfit weights to one module (rigor #2 steelman). Surfaced as an honest divergence rather than gamed. Both independent verifiers (GPT-5.5 + verifier-critic) call this a fair judgment, not a cop-out. OPEN QUESTION for the SPR author: is the results.py anchor an over-strong intuition, or should the proxy credit framework-delegated (pydantic/dataclass) depth?",
    "churn conflation: rank_score = ratio x log1p(churn) is the spec's formula (shallow AND hot). A consequence is that a very hot but only median-shallow module (events.py churn 17, search.py churn 8) can rank above a shallower but cooler one. By design, but the head is a shallow-AND-hot PRIORITY list, not a pure shallowness ranking. Every row carries both `ratio` and `rank_score`; ranking.md now labels the head accordingly."
  ],
  "independent_corroboration": {
    "gpt5.5_codex": "Agreed results.py is 'moderately shallow / acceptable utility wrapper, not a deep module; forcing it deep would be gaming the metric.' Flagged 0.5/0.5 weights as possibly calibration-driven -> actioned by emitting flat_ratio alongside weighted.",
    "verifier_critic": "SOUND-WITH-CAVEATS. On its OWN adversarially-drawn 10-module sample: ratio-agreement 10/10 (the pure Ousterhout signal correct in every module); rank_score-position 6/10 (all 4 misses are the disclosed churn-conflation). Confirmed anti-radon core, safe/reproducible instrument, no AST miscounts. Caught the fence-prefix MAJOR (now fixed) as its only load-bearing finding."
  }
}
