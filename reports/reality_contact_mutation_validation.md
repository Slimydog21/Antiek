# Reality-contact mutation-probe validation report

Generated by `python -m tools.reality_contact.mutation_probe`. This is the EMPIRICAL validator for SPR-01's static reality/theater labels: it injects a fault into a real CORE symbol and observes whether a `reality`-labeled test goes RED (it really exercised the core) or sails through GREEN (it never touched the core on that path — a candidate false positive). It does NOT edit the tree: every mutation is injected at runtime in a throwaway subprocess plugin and restored; nothing here is committed.

## How to read this (the fairness split is load-bearing)

A `reality` test passing under the fault is a CANDIDATE false positive only if it actually **references the faulted symbol** — otherwise the fault was on a core path *this* test does not drive (a **shallow fault**), which would unfairly indict the classifier. So every per-subsystem table separates `classifier-fp` (passed AND references the symbol) from `shallow-fault` (passed, does not reference it). The false-positive RATE is computed only over **fairness-eligible** outcomes (reality tests that reference the symbol).

The false-NEGATIVE half is validated on a **synthetic theater control**, not the real tree: this branch has `theater == 0` (no real theater tests to sample), so for each subsystem a synthetic test that mocks the faulted symbol and asserts the mock is run under the fault. It MUST stay GREEN — proving the probe's FN-detection direction works. A RED here would mean the probe itself is broken, not that the real suite has a false negative.

## Probe self-correction (why the number is trustworthy)

An early naive version of this probe reported an overall FP rate of **16.67%** — which on inspection was ENTIRELY probe artifact, not classifier error, and fixing it is the rigor of this sprint:

1. **Coarse fairness check.** The first version matched the symbol name as a regex word, so three router tests that mention `dispatch` only in their DOCSTRINGS (they import `register_provider` / `_PROVIDER_REGISTRY`, never `dispatch`) were falsely counted fairness-eligible and, on passing, scored false positives. The fix: the fairness check is now AST-based — it requires a real import or call of the symbol; prose mentions are invisible to it. Counting a docstring mention as evidence the test drives the symbol is exactly the *"shallow fault unfairly indicts the classifier"* error this sprint forbids.

2. **Incomplete fault site.** `tests/test_graph.py` genuinely calls `search()` yet passed under a faulted `substrate.graph.search.search`, because it imports via `from substrate.graph import search` — the PACKAGE re-export binding, which a module-only patch never touched. That made a real-contact test look like a false positive. The fix: the fault is now installed at every same-identity binding site (defining module + re-export packages), so a test importing the symbol either way sees it. After the fix `test_graph.py` correctly goes RED.

Both corrections REMOVED spurious false positives (they did not hide real ones): every probed test that still passes is now a genuine, fairly-attributed candidate. The reproduced number below is post-correction.

## Headline

- **Overall false-positive rate (over fairness-eligible reality outcomes) = 0.00%**
- Sample: up to N=5 reality tests per subsystem (see per-subsystem tables).
- SCOPE (read before quoting the 0%): this rate is over the actually-EXECUTED fairness-eligible tests only — a label SPOT-CHECK, not a full-suite census. Every capped or excluded test is listed by name per subsystem below (no silent skips), and per-subsystem evidence strength varies (see each fault's rationale — schema's is the weakest, search/dispatch the strongest).
- Theater FN-half: validated on synthetic theater controls (6/6 stayed green as required).

## Per-subsystem results

### `substrate.graph.ops`

- **Fault injected:** `substrate.graph.ops.insert_document` — mode `raise`.
- **Why this is a valid corruption:** insert_document is the primary graph WRITE entry point: a test that really writes a document/chunk/node to the substrate calls it on its setup path. Making it raise means no real node is written, which any test asserting against the written graph propagates to a failure. Genuinely imported/called (AST) by 34/48 graph.ops reality tests.
- **Sample:** 5 of 48 reality tests probed (sorted-by-path; restrict to tests that genuinely import/call 'insert_document' (AST, ignoring prose mentions); first 5 probed).
- **False-positive rate:** 0.00% (0 classifier-fp / 5 fairness-eligible).
- **Capped (eligible, un-probed only by the N=5 cap; logged, no silent skips): 29 test(s)** —
  - `tests/test_block_repository.py`
  - `tests/test_book_corpus_gate.py`
  - `tests/test_book_curate.py`
  - `tests/test_compliance_invariants.py`
  - `tests/test_get_chunk_personal_reading.py`
  - `tests/test_library_api.py`
  - `tests/test_marginalia_promotion.py`
  - `tests/test_merge_staging.py`
  - `tests/test_monetization_eligibility.py`
  - `tests/test_monitoring_mode.py`
  - `tests/test_null_content_class_gate.py`
  - `tests/test_outline_block.py`
  - `tests/test_passage_research.py`
  - `tests/test_personal_reading_lane.py`
  - `tests/test_promote_context.py`
  - `tests/test_provenance_economics_surfaced.py`
  - `tests/test_read_ad_escrow.py`
  - `tests/test_register_source.py`
  - `tests/test_retrieval_gate_canon.py`
  - `tests/test_retrieval_gate_closure.py`
  - `tests/test_retrieval_gate_matrix.py`
  - `tests/test_retrieval_substrate_personal_reading.py`
  - `tests/test_rights_audit.py`
  - `tests/test_servability_drift_guard.py`
  - `tests/test_serve_guard.py`
  - `tests/test_sprint11_api.py`
  - `tests/test_trace_to_source.py`
  - `tests/test_write_routes.py`
  - `tests/test_x_byok_training_exclusion.py`
- **Not fairness-eligible (14 test(s)): reality-labeled but they do NOT directly import/call the faulted symbol (they reach it indirectly, e.g. via DI/the orchestrator), so the probe cannot attribute this fault to them per-test. NOT counted as FP or as classifier-correct — honestly out of this probe's reach** —
  - `tests/test_async_interview.py`
  - `tests/test_brainstorm_interview.py`
  - `tests/test_contracts_conformance.py`
  - `tests/test_flywheel_reuse_gate.py`
  - `tests/test_gap_detection.py`
  - `tests/test_graph_insight_question.py`
  - `tests/test_knowledge_reuse_spr06.py`
  - `tests/test_pdf_storage_tiering.py`
  - `tests/test_reclassify_personal_reading.py`
  - `tests/test_speak_consent.py`
  - `tests/test_speak_drw_gap_source.py`
  - `tests/test_speak_fk_regression.py`
  - `tests/test_sprint14_api.py`
  - `tests/test_unit_dedup.py`

| test | references symbol? | passed under fault? | verdict |
| --- | :---: | :---: | --- |
| `tests/test_ad_routes.py` | yes | RED | `expected-red` |
| `tests/test_arxiv_audit.py` | yes | RED | `expected-red` |
| `tests/test_arxiv_index.py` | yes | RED | `expected-red` |
| `tests/test_attribution.py` | yes | RED | `expected-red` |
| `tests/test_backtest_db.py` | yes | RED | `expected-red` |

### `substrate.graph.schema`

- **Fault injected:** `substrate.graph.schema.init_database` — mode `raise`.
- **Why this is a valid corruption:** init_database creates the real DuckDB tables a schema-claiming test must exercise. Raising it means the schema is never built, so any subsequent real query fails. Genuinely imported/called (AST) by 52/84 schema reality tests. WEAKEST-EVIDENCE CAVEAT (be honest about this subsystem): init_database sits on the SHARED DB-setup path, so it is triggered TRANSITIVELY by virtually any DB-touching test — e.g. test_attribution / test_backtest_db go RED under it even though they do NOT AST-reference it. So a RED here proves 'this test needs a working database', NOT 'this test exercises schema-CREATION specifically'. schema is the LEAST surgically-targeted subsystem fault; contrast search.search (retrieval-specific) or dispatch.dispatch (the routing decision itself), where a RED is stronger evidence of contact with that exact subsystem. The headline 0-percent FP is unaffected (only symbol-referencing tests are counted), but schema's reds carry less evidential weight.
- **Sample:** 5 of 84 reality tests probed (sorted-by-path; restrict to tests that genuinely import/call 'init_database' (AST, ignoring prose mentions); first 5 probed).
- **False-positive rate:** 0.00% (0 classifier-fp / 5 fairness-eligible).
- **Capped (eligible, un-probed only by the N=5 cap; logged, no silent skips): 47 test(s)** —
  - `tests/test_async_interview.py`
  - `tests/test_attribution_audit.py`
  - `tests/test_backfill_cc0_remap.py`
  - `tests/test_billing_kyc.py`
  - `tests/test_billing_tax_reports.py`
  - `tests/test_biography_authoring.py`
  - `tests/test_book_corpus_gate.py`
  - `tests/test_book_curate.py`
  - `tests/test_compliance_invariants.py`
  - `tests/test_compounding_interviewer.py`
  - `tests/test_contributor_economics.py`
  - `tests/test_cross_interviewee.py`
  - `tests/test_economics_matrix.py`
  - `tests/test_flywheel_reuse_gate.py`
  - `tests/test_graph_insight_question.py`
  - `tests/test_ip_holder_resolver.py`
  - `tests/test_ip_holders.py`
  - `tests/test_knowledge_reuse_spr06.py`
  - `tests/test_merge_staging.py`
  - `tests/test_nonce_ledger_persistence.py`
  - `tests/test_notebooks.py`
  - `tests/test_null_content_class_gate.py`
  - `tests/test_partner_identity_persistence.py`
  - `tests/test_passage_research.py`
  - `tests/test_payout_decisions_log.py`
  - `tests/test_personal_reading_lane.py`
  - `tests/test_public_graph_quality_gate.py`
  - `tests/test_read_ad_escrow.py`
  - `tests/test_reclassify_personal_reading.py`
  - `tests/test_register_source.py`
  - `tests/test_retrieval_gate_canon.py`
  - `tests/test_retrieval_gate_closure.py`
  - `tests/test_retrieval_gate_matrix.py`
  - `tests/test_retrieval_substrate_personal_reading.py`
  - `tests/test_retrieval_time_gate.py`
  - `tests/test_rollover_persistence.py`
  - `tests/test_serve_guard.py`
  - `tests/test_speak_biography_composition.py`
  - `tests/test_speak_compounding_interviewer.py`
  - `tests/test_speak_consent.py`
  - `tests/test_speak_drw_gap_source.py`
  - `tests/test_speak_fk_regression.py`
  - `tests/test_speak_payout_verifier.py`
  - `tests/test_speak_project.py`
  - `tests/test_speak_publish.py`
  - `tests/test_speak_write_composer.py`
  - `tests/test_unit_dedup.py`
- **Not fairness-eligible (32 test(s)): reality-labeled but they do NOT directly import/call the faulted symbol (they reach it indirectly, e.g. via DI/the orchestrator), so the probe cannot attribute this fault to them per-test. NOT counted as FP or as classifier-correct — honestly out of this probe's reach** —
  - `tests/test_async_note_taker.py`
  - `tests/test_attribution.py`
  - `tests/test_backtest_db.py`
  - `tests/test_block_repository.py`
  - `tests/test_brainstorm_interview.py`
  - `tests/test_cascade_planner.py`
  - `tests/test_contracts_conformance.py`
  - `tests/test_corpus_audit.py`
  - `tests/test_cost_consent_no_disbursement.py`
  - `tests/test_gap_detection.py`
  - `tests/test_get_chunk_personal_reading.py`
  - `tests/test_health_flywheel.py`
  - `tests/test_ingest_observability.py`
  - `tests/test_launch_n_spr05.py`
  - `tests/test_legal_gate.py`
  - `tests/test_marginalia_promotion.py`
  - `tests/test_max_context_pack.py`
  - `tests/test_middleware_backtest.py`
  - `tests/test_monetization_eligibility.py`
  - `tests/test_monitoring_mode.py`
  - `tests/test_outline_block.py`
  - `tests/test_parallel_orchestration.py`
  - `tests/test_personal_lane_read_side.py`
  - `tests/test_promote_context.py`
  - `tests/test_provenance_economics_surfaced.py`
  - `tests/test_remote_exec_isolation.py`
  - `tests/test_research_runner.py`
  - `tests/test_sprint11_api.py`
  - `tests/test_sprint14_api.py`
  - `tests/test_sprint16_interviews.py`
  - `tests/test_trace_to_source.py`
  - `tests/test_universal_ingest.py`

| test | references symbol? | passed under fault? | verdict |
| --- | :---: | :---: | --- |
| `tests/test_ad_inventory_persistence.py` | yes | RED | `expected-red` |
| `tests/test_advertiser_onboarding_persistence.py` | yes | RED | `expected-red` |
| `tests/test_ai_actions.py` | yes | RED | `expected-red` |
| `tests/test_arxiv_ad_events.py` | yes | RED | `expected-red` |
| `tests/test_arxiv_audit.py` | yes | RED | `expected-red` |

### `substrate.graph.search`

- **Fault injected:** `substrate.graph.search.search` — mode `raise`.
- **Why this is a valid corruption:** search() is the vector-retrieval core. A retrieval test that really runs the index calls it; raising means no real retrieval occurs, which a provenance/retrieval assertion propagates to a failure. Genuinely imported/called (AST) by 10/10 graph.search reality tests. NOTE: the fault is installed at BOTH substrate.graph.search.search AND the substrate.graph package re-export, because tests import it via 'from substrate.graph import search' (the package binding) — patching only the module would miss them and falsely score a false positive.
- **Sample:** 5 of 10 reality tests probed (sorted-by-path; restrict to tests that genuinely import/call 'search' (AST, ignoring prose mentions); first 5 probed).
- **False-positive rate:** 0.00% (0 classifier-fp / 5 fairness-eligible).
- **Capped (eligible, un-probed only by the N=5 cap; logged, no silent skips): 5 test(s)** —
  - `tests/test_retrieval_gate_canon.py`
  - `tests/test_retrieval_gate_closure.py`
  - `tests/test_retrieval_gate_matrix.py`
  - `tests/test_retrieval_substrate_interface.py`
  - `tests/test_retrieval_time_gate.py`

| test | references symbol? | passed under fault? | verdict |
| --- | :---: | :---: | --- |
| `tests/test_book_corpus_gate.py` | yes | RED | `expected-red` |
| `tests/test_graph.py` | yes | RED | `expected-red` |
| `tests/test_monitoring_mode.py` | yes | RED | `expected-red` |
| `tests/test_null_content_class_gate.py` | yes | RED | `expected-red` |
| `tests/test_personal_reading_lane.py` | yes | RED | `expected-red` |

### `substrate.dispatch.router`

- **Fault injected:** `substrate.dispatch.router.dispatch` — mode `raise`.
- **Why this is a valid corruption:** dispatch() IS the routing decision a dispatch test exists to prove. Raising it means no routing/cost decision is made; a test asserting on the dispatch result fails. IMPORTANT HONESTY NOTE: only 2/17 router reality tests genuinely import/call dispatch directly (AST). The other 15 fake the PROVIDER by DI (register_provider(stub) — a BOUNDARY seam) and reach routing INDIRECTLY through the orchestrator/synthesizer, so the probe cannot attribute a dispatch fault to them per-test and they are (fairly) NOT counted as fairness-eligible. This small eligible sample is exactly the SPR-06 DI blind spot surfacing in the probe's own coverage: the router decision is still real for those 15, but the probe can only PROVE it for the 2 that call dispatch directly.
- **Sample:** 2 of 17 reality tests probed (sorted-by-path; restrict to tests that genuinely import/call 'dispatch' (AST, ignoring prose mentions); first 5 probed).
- **False-positive rate:** 0.00% (0 classifier-fp / 2 fairness-eligible).
- **Not fairness-eligible (15 test(s)): reality-labeled but they do NOT directly import/call the faulted symbol (they reach it indirectly, e.g. via DI/the orchestrator), so the probe cannot attribute this fault to them per-test. NOT counted as FP or as classifier-correct — honestly out of this probe's reach** —
  - `tests/test_connector_bridge.py`
  - `tests/test_dispatch_bootstrap.py`
  - `tests/test_dispatch_synthesis_pin.py`
  - `tests/test_evidence_retriever_bridge.py`
  - `tests/test_extraction.py`
  - `tests/test_grounding.py`
  - `tests/test_loop_one_concurrent.py`
  - `tests/test_loop_one_orchestrator.py`
  - `tests/test_note_taking.py`
  - `tests/test_parameter_extractor_bridge.py`
  - `tests/test_provider_anthropic.py`
  - `tests/test_roles_decomposer_extraction.py`
  - `tests/test_synthesizer_bridge.py`
  - `tests/test_synthesizer_measurement_window_14_4.py`
  - `tests/test_wrestling.py`

| test | references symbol? | passed under fault? | verdict |
| --- | :---: | :---: | --- |
| `tests/test_dispatch_fallback_chain.py` | yes | RED | `expected-red` |
| `tests/test_research_tier_dispatch.py` | yes | RED | `expected-red` |

### `substrate.attribution.compute`

- **Fault injected:** `substrate.attribution.compute.compute_attribution_for_synthesis` — mode `raise`.
- **Why this is a valid corruption:** compute_attribution_for_synthesis is the §9 IP-attribution core. Raising it means no attribution is computed; an attribution test fails. Referenced by 3/3 attribution reality tests.
- **Sample:** 3 of 3 reality tests probed (sorted-by-path; restrict to tests that genuinely import/call 'compute_attribution_for_synthesis' (AST, ignoring prose mentions); first 5 probed).
- **False-positive rate:** 0.00% (0 classifier-fp / 3 fairness-eligible).

| test | references symbol? | passed under fault? | verdict |
| --- | :---: | :---: | --- |
| `tests/test_personal_reading_lane.py` | yes | RED | `expected-red` |
| `tests/test_provenance_economics_surfaced.py` | yes | RED | `expected-red` |
| `tests/test_retrieval_gate_closure.py` | yes | RED | `expected-red` |

### `orchestration.loop_one.orchestrator`

- **Fault injected:** `orchestration.loop_one.orchestrator._score_phase_6_synthesis` — mode `raise`.
- **Why this is a valid corruption:** _score_phase_6_synthesis is a real groundedness-scoring step inside the loop-1 orchestrator. The two orchestrator reality tests exercise DIFFERENT private functions (groundedness uses this; chase uses _walk_chase_chain / _select_chase_question), so the per-test fairness check is especially load-bearing here: a test not referencing this symbol is shallow-fault, not a classifier error.
- **Sample:** 1 of 2 reality tests probed (sorted-by-path; restrict to tests that genuinely import/call '_score_phase_6_synthesis' (AST, ignoring prose mentions); first 5 probed).
- **False-positive rate:** 0.00% (0 classifier-fp / 1 fairness-eligible).
- **Not fairness-eligible (1 test(s)): reality-labeled but they do NOT directly import/call the faulted symbol (they reach it indirectly, e.g. via DI/the orchestrator), so the probe cannot attribute this fault to them per-test. NOT counted as FP or as classifier-correct — honestly out of this probe's reach** —
  - `tests/test_orchestrator_chase.py`

| test | references symbol? | passed under fault? | verdict |
| --- | :---: | :---: | --- |
| `tests/test_orchestrator_groundedness.py` | yes | RED | `expected-red` |

## Synthetic theater controls (FN-direction validation)

| subsystem | faulted symbol | stayed green? | verdict |
| --- | --- | :---: | --- |
| `substrate.graph.ops` | `substrate.graph.ops.insert_document` | yes | `expected-green` |
| `substrate.graph.schema` | `substrate.graph.schema.init_database` | yes | `expected-green` |
| `substrate.graph.search` | `substrate.graph.search.search` | yes | `expected-green` |
| `substrate.dispatch.router` | `substrate.dispatch.router.dispatch` | yes | `expected-green` |
| `substrate.attribution.compute` | `substrate.attribution.compute.compute_attribution_for_synthesis` | yes | `expected-green` |
| `orchestration.loop_one.orchestrator` | `orchestration.loop_one.orchestrator._score_phase_6_synthesis` | yes | `expected-green` |

## Gate recommendation (feeds SPR-06 reconsider-if)

SPR-06 (`docs/decisions/reality-contact-ratio.md`, §3.3) recorded the reconsider-if verbatim: *"If SPR-07's mutation probe demonstrates the static classifier has a HIGH FALSE-POSITIVE rate — it red-flags tests that do make genuine reality contact — then downgrading this gate to informational becomes justified."* Note the directionality: a HIGH FP rate here means reality tests PASS under a broken core (the classifier called them reality but they make no contact), which is the OPPOSITE concern — it means the gate's `reality` label is too generous, not that it over-flags genuine contact. Either way the measured number is the input the operator weighs.

- **Recommendation: HOLD blocking.** Overall FP rate 0.00% is LOW: on the stated sample, reality tests overwhelmingly DO go red when their core breaks, so the `reality` label is empirically trustworthy and the SPR-06 reconsider-if is NOT triggered.

> Scope caveat carried from SPR-06: this probe samples the labels; it is NOT full mutation testing of the suite, and it is structurally silent on the DI-stubbed-LLM blind spot (the provider is a BOUNDARY seam, so a test that fakes the LLM by dependency injection still runs the real graph/orchestrator and is correctly `reality` for THOSE subsystems). The FP rate here is about whether a `reality` label survives a broken CORE symbol, not about whether the synthesis content was real.

