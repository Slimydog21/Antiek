/**
 * @deprecated SPR-08 — folded into UnifiedSearch. Do not import in new code.
 * Retained only so historical tests can migrate; production entry is UnifiedSearch.
 */
export { default } from "../../components/UnifiedSearch";
export type { UnifiedSearchProps as CorpusSearchProps } from "../../components/UnifiedSearch";