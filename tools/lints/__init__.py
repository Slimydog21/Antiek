"""AST-level lints for Antiek substrate discipline.

See ``tools/lints/no_raise_in_substrate_writers.py`` and
``tools/lints/unannotated_bypass.py``. Both are opt-in per path (the
operator decides when to wire them into CI).
"""
