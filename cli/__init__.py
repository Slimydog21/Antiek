"""CLI entry points for Antiek operator commands.

Each subcommand lives in its own module (``library_ingest.py``,
``library_ls.py``, etc.) and exposes a ``main(argv)`` entry. The
top-level ``antiek`` command dispatches by first positional arg.
"""
