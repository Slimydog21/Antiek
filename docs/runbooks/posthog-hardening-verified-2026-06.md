# PostHog Analytics Hardening — VERIFIED report (2026-06-04)

**Capstone:** APH SPR-06. **Integration branch:** `caffen/aph-integration` (all five sprint branches merged cleanly off `origin/main`). **Status:** ✅ VERIFIED — every gate green *together* in one tree, each non-vacuous gate proven to bite.

This report is the single artifact an operator reads to trust the whole. Every "pass" below is backed by a command I ran in the integrated tree; the bot-filter invariant (INV-1) is honored throughout (capture is proven **server-side**, never from a browser probe).

## Gate results (run in the integrated tree)

| Gate | Sprint | Command | Result |
|---|---|---|---|
| A · Taxonomy completeness | SPR-01 | `npx vitest run src/lib/taxonomy.gate.test.ts` | **4/4 pass** |
| B · §9.0 firewall suite | SPR-04 | `npx vitest run src/lib/posthogClient.test.ts` | **37/37 pass** |
| C · Typecheck | — | `npx tsc -b` | **exit 0** |
| D · Capture (server-side) | SPR-02 | `python3 tools/posthog/verify_capture.py` | **PASS** — sentinel confirmed via the events API (exact `verify_run_id`) |
| E · Dashboard idempotency | SPR-03 | `sync_dashboards.py --apply` ×2 + `--check` | **no-op (idempotent) + round-trip OK** |

> Path anchors: gates **A/B/C** run from `apps/reading/` (the vitest/tsc root) — the `src/lib/...` paths are relative to there. Gates **D/E** run from the repo root (`tools/posthog/...`), with `POSTHOG_PROJECT_TOKEN` (phc_) + `POSTHOG_PERSONAL_API_KEY` (phx_) in the environment.

## Negative controls (proof the gates are non-vacuous)

A green suite that has never been shown to fail proves nothing. Both bite:

| Control | Injection | Result | Restored |
|---|---|---|---|
| Taxonomy bites | a scratch `track("totally_fake_capstone_event")` in `src/` | **RED** — 1 failed, named the orphan + the file + the fix | scratch removed → 4/4 |
| Firewall bites | neutralized `scrubProperties` (early `return`) | **RED** — 16 firewall tests failed | restored → 37/37 |

(SPR-02's verifier was independently shown to bite earlier: a never-emitted `run_id` correctly FAILs — no false-match.)

## What this does NOT cover (honesty boundary — do not over-read the VERIFIED stamp)

- **Prod human-user capture is *inferred*, not directly observed.** That posthog captures real visitors' events is established by (a) the server-side sentinel landing in the events API and (b) INV-1 (automated browsers are bot-filtered, humans are not) — NOT by a live human browser session. A real human loading `antiek.ai` and the event appearing in the North Star dashboard is the only fully-direct confirmation; it is left to the operator.
- **SPR-03 deferrals:** `--apply` does not yet *prune* tiles present on the live dashboard but absent from the contract (off the fresh-project recovery path); and an apply→check round-trip on a genuinely fresh project (vs the current fixed-point contract) is an untested-but-worthwhile probe. Insight reconciliation is by NAME — a live rename produces a duplicate, not a reconcile (documented).
- **SPR-04 deferral:** the `$elements_chain` scrub is grammar-coupled to posthog-js's current serialization; the version-robust successor is to drop/skeletonize the chain rather than scrub it. Three review rounds each closed a deeper chain leak (`attr__href`, all `attr__*`, escaped quotes) — current scrub is complete for the known grammar, but a posthog grammar change is the reconsider trigger.
- **SPR-02 endpoint:** verification uses the (deprecated-but-real-time) events list, not HogQL — HogQL/ClickHouse ingest lag left a fresh sentinel invisible >90s in live testing, so it is unfit for real-time sentinel verification (evidence in the tool docstring).
- **Not merged to `main`.** Every sprint lives on its `caffen/aph-*` branch; the integration branch is `caffen/aph-integration`. Merge to `main` + any prod deploy is operator-gated and has not been done.

## Provenance

| Sprint | Branch | Commit | Rounds |
|---|---|---|---|
| SPR-01 taxonomy gate | `caffen/aph-spr-01` | `c72e730` | 2 |
| SPR-04 firewall hardening | `caffen/aph-spr-04` | `30d949d` | 3 (closed 3 real §9.0 leaks) |
| SPR-05 decision records + runbook | `caffen/aph-spr-05` | `b206a0d` | 2 |
| SPR-02 capture verifier | `caffen/aph-spr-02` | `6549426` | 2 (caught+reverted a live HogQL regression) |
| SPR-03 dashboards-as-code | `caffen/aph-spr-03` | `60180f8` | 2 (BLOCKER closed by live throwaway proof) |
| SPR-06 capstone | `caffen/aph-integration` | (this) | — |

Generated by `/caffenagent` executing `~/specs/antiek-posthog-hardening/`. Run ledger: `.caffenagent/state.json`.
