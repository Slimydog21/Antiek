# Werner dusk-gaze beat — cycle decision

Date: 2026-07-14
Sprint: WERNER-ACT SPR-29

## Decision

Ship one identity-anchored ChatGPT Image dusk-gaze pose and transport the
already-modeled `dusk-settle` edge through Scene's existing one-way shell seam.
The sole live Werner may show it once for 1400 ms, subject to the same strict
foreground precedence as dawn, then return to his existing station state.

The pose is private presentation art, not a fifth canonical mood. Scene remains
the only time and theme authority; AppShell transports an opaque identified cue;
PenguinMascot remains the only foreground arbiter.

## Proof

- ChatGPT Image used canonical idle and thinking art as identity references.
  The generated RGB checkerboard source is provenance-only; the 1024×1024
  runtime PNG has real RGBA alpha and an opaque, preserved belly.
- Ten coupled suites pass 77 tests covering transition cardinality, monotonic
  dusk/dawn identity, legacy dawn compatibility, foreground preemption,
  reduced motion, authored art, and fixed-station behavior.
- TypeScript, token lint, production build, Storybook build, and diff check
  pass.
- The fidelity plate is zero-diff at 768, 1024, and 1280 pixels across repeated
  runs. The expanded 50-story axe audit passes after correcting one real text
  contrast defect.
- A fresh GPT-5.6-sol critic returned APPROVE with no blocking findings.
  HardenX strict reports LOW with 0 REAL findings and 14 repository advisories.

## Withheld authority

No fifth mood, second mascot, new clock, theme listener, global event, stale
replay, persistent dusk state, station movement, content mutation, route change,
network or provider call, video stream, merge, or deployment is authorized.
