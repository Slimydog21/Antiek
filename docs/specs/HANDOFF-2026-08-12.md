# HANDOFF — Antiek Sharpen & Perfect Goal Instance (2026-08-12)

**Author**: Prime Agent (goal instance: sharpen/perfect Antiek v1 website).
**Date**: 2026-08-12 (session ~12:00–17:00 UTC).
**Next-agent entry point**: read this file, then per-area files listed below.
**Repo base**: github.com/Slimydog21/Antiek @ main tip `2e4e788d2` (#3032).

---

## 0. What this instance did

1. **Read the historical corpus** (master-product-spec.md §1–§17 incl. §4/§5/§9/§13/§16,
   rlm_integration_spec.md, philosophy canon, integration specs, forensic reports
   2026-08-11/12, ~/.claude/skills/arxiv, frontend-craft skill).
2. **Audited main** (worktree at 2e4e788d2) against every operator directive.
3. **Executed a 5-model swarm** (codex/grok/glm/deepseek/mimo — kimi was
   quota-blocked) on the gaps + wrote inline specs for the rest.
4. **Opened PR #3033** (metered Prime Agent foundation) and prepared the
   remaining branches/PRs (see §3).

## 1. Directive-by-directive status (verified 2026-08-12)

### 1.1 Prime Agent in all RLM workflows — ✅ mostly built, PR #3033 adds metering
- Merged on main: `runtime/remote_exec/prime_exec.py` (PrimeExecProvider RPC
  adapter, default-off, #2983), bounded evidence lane (#3005), `prime_agent_backend.py`
  (REPL/DAG/tool-batch/long-corpus/investigation wiring).
- **NEW (PR #3033, 125 tests pass)**: `runtime/prime_agent/` (installation +
  process), `orchestration/rlm/prime_authority.py` ($5/session cap, replay
  protection), `prime_ledger.py` (durable metering), `prime_rpc_evidence.py`.
- Remaining: operator ratify + flip `ANTIEK_PRIME_AGENT_RLM_ENABLED` when
  desired; production model/provider selection UI (per htmlspec open question).

### 1.2 BYOT token onboarding + remove operator keys — ✅ mechanism built; ⏳ operator runbook pending
- Merged: BYOK-first dispatch (#3000, `resolve_provider_key`), first-party
  provider keys UI (#3019), Grok OAuth device flow (#2997 + runtime/byok/grok_oauth.py),
  OpenAI/Anthropic presets (#3018), per-key usage/balance server routes
  (byot_usage_routes.py), model inventory + budget (Settings/AddModelPanel),
  ANTIEK_BYOT_ONLY env-gate.
- **In flight**: usage/balance dashboard UI (codex) + OpenAI/Claude OAuth (glm).
- **Operator action runbook (this goal instance)**: see §4 — store provider
  keys via Settings → AddModelPanel (BYOK SecretBox), then set
  `ANTIEK_BYOT_ONLY=1` in /etc/antiek/secrets.env and remove the provider keys
  (DEEPSEEK_API_KEY, HERMES_API_KEY, XIAOMI_API_KEY, Z_AI_API_KEY, EXA_API_KEY,
  SERPAPI_API_KEY, BROWSERBASE_API_KEY, AGENTMAIL… as applicable) from the env
  file. Non-provider keys stay (STRIPE, CF_ACCESS, signing keys).

### 1.3 Usage dashboard + balance + dropdown model selection — 🟡 backend done, frontend in flight
- Server: `GET /settings/usage`, `POST /settings/usage/{id}/limit`,
  `GET /settings/balance/{id}` (deepseek + kimi balance adapters).
- Frontend: ModelDecisionBar (per-prompt model + budget projection) exists;
  **UsagePanel + balance chips in dropdowns being added by codex child**.

### 1.4 arXiv verified + skill-aligned — ✅ endpoint fixed (#3006); 🔨 verifier in flight
- Main fix merged; server patched; sync pages oaipmh.arxiv.org (200 OK).
- Known prod gap: nightly sync never completes (full re-crawl, 6h timeout,
  no state file). mimo child is building `tools/arxiv_verify.py` + runbook +
  audit against ~/.claude/skills/arxiv discipline.

### 1.5 HTML-first artifacts + style wheel — ✅ built; UX completed this instance
- Merged: anydoc/OCR ingestion arms (#2996/#3002/#3021), style backend
  (style_routes.py: wheel/create/fork/delete/render), StyleWheel.tsx.
- **NEW (grok child, verified 21/21 tests + tsc clean)**: seeded fork flow,
  delete-fork UI, provenance chips, empty state, `deleteStyle` client,
  docs/design-system.md + docs/html-first-design-thesis.md.
- Backend gap flagged by child: `StyleOut` lacks `parent`/`forked_from` — real
  cross-session provenance needs a schema addition (small, spec'd in PR body).

### 1.6 Publisher products + TollBit — 📄 specs delivered (deepseek child)
- `docs/specs/publisher-ecosystem-2026-08-12.md` (~3.4k words): marketplace
  (Stripe Checkout self-hosted v1), research-ingestion API product, licensing
  per Bartz, Spotify/Amazon precedents, luring mechanics (12% first-3 equity,
  ad rev-share, opt-in escrow).
- `docs/specs/tollbit-bridge-2026-08-12.md` (~3.4k words): TollBit model
  (~7k publishers, Licensed Search/Content Token/Metering, $0.001–0.20/page),
  verdict: short/medium-term bridge, ad-revenue-first endgame, decision matrix.

### 1.7 Ad infra + per-second attribution — ✅ substrate exists; 📄 phase plan in master spec
- Merged: ad_routes/ad_impressions/advertisers/creator_payouts,
  ad-fill decisions (#3025), AppLovin auction features, verify_ad_economics.py,
  frame-attribution spec in specs/antiek-frame-attribution.
- Per-second attribution = master-spec §9 phased plan (telemetry first,
  manual sponsor, publisher dashboard). No new build needed until publisher
  opt-in (legal gate §9.0).

### 1.8 BYOTools (X/YouTube/vendors) — ✅ X+YouTube built; 📄 expansion spec
- Merged: settings_tool_connections + research_tool_search (X, YouTube with
  quota honesty) (#3026/#3013).
- **NEW spec**: docs/specs/byot-tools-vendors-2026-08-12.md — tiered vendor
  roadmap (Google Drive/Notion/RSS/GitHub/FRED/Qualtrics) + honesty note that
  sell-side/expert APIs are enterprise-gated (user-upload + DeepBlu is the
  compliant path).

### 1.9 Daytona sandboxes — ✅ seam built + spec; 📄 scalability roadmap
- runtime/remote_exec provider seam (Daytona + Prime adapters), factory,
  cost events; docs/daytona_integration_spec.md (rejection-first admissions).
- **NEW**: docs/specs/scalability-roadmap-2026-08-12.md — capacity envelope,
  the sandbox question answered (sandboxes for untrusted tool exec, not the
  substrate), staged plan 0→2 with the operator's literal questions answered.

### 1.10 Turbopuffer sharding — 📄 addendum spec
- **NEW**: docs/specs/turbopuffer-sharding-2026-08-12.md — verified Namespace
  Sharding facts (hash(id)%N, shared WAL, strong consistency preserved, sizing
  1TB/shard/256TB max, num_shards set at first write), decision: set num_shards=2
  at Wedge-1 first write, guardrails.

### 1.11 Processing (Casey Reas) — 📄 spec
- **NEW**: docs/specs/processing-visual-design-2026-08-12.md — p5.js style lane
  + data-viz lane + processing.py offline cover renders; REJECTs (no JVM in
  substrate, no p5 on every page, deterministic seeds).

### 1.12 Account-level memory + anydoc agent memory — ✅ built
- Merged: account_memory_* routes (#3028), thought-partner owner context
  (#3014), tools/antiek_memory/server.py (MCP server), research graph +
  events. Grok's html-first thesis doc covers agent-facing memory design.

## 2. Website product-surface audit (verified against App.tsx routes)

All five master-spec surfaces are live routes: Research Workstation `/`,
Deep Research `/deep-research`, Wrestle `/wrestle`, Write/Create `/write`,
Brainstorm `/brainstorm`, Speak `/speak`, plus Library/Documents/Notebooks/
Settings/Billing/Stats/Map/Multimedia/Pricing/Privacy/TrustCenter/Operator.

**"Begin using it" blockers found this instance** (in priority order):
1. `antiek-health-probe` dead since May 18 + dead alert webhook (no monitoring).
2. 17 apt updates + reboot pending since Aug 6; no fail2ban.
3. Backup freshness marker still Aug 7 until tonight's v2 run (verify Aug 13 06:30 probe).
4. arXiv nightly sync never completes (full re-crawl, 6h timeout).
5. Operator keys still in secrets.env (BYOT-only migration not done — §4).
6. Deploy-source hazard: canonical ~/Antiek/platform templates are stale v1 —
   always deploy from a fresh main worktree until fixed.

## 3. PRs / branches

| PR | Content | State (verified) |
|---|---|---|
| **#3033** feat/prime-agent-metered-foundation | metered Prime foundation (authority/ledger/process/rpc-evidence) | OPEN — 125 tests pass |
| **#3034** feat/style-wheel-fork-provenance-ux | StyleWheel fork/delete/provenance UX + docs/design-system.md + docs/html-first-design-thesis.md | OPEN — 21 vitest + tsc clean |
| **#3035** docs/sharpen-specs-2026-08-12 | 7 specs (scalability, turbopuffer-sharding, processing, byot-tools, publisher-ecosystem, tollbit-bridge, HANDOFF) | OPEN |
| **#3036** feat/byot-usage-dashboard-ui | UsagePanel + balance chips (codex child; verified by parent after child interrupted) | OPEN — 29 vitest + tsc clean |
| **#3038** feat/arxiv-verifier | tools/arxiv_verify.py + 17 tests + ops runbook | OPEN — 41 pytest; live endpoint PASS |
| **#3039** feat/byot-oauth-openai-anthropic | OpenAI + Anthropic PKCE OAuth + spec (glm child) | OPEN — 137 pytest |

## 4. Operator runbook — migrate own keys to BYOT (10 min)

1. Open https://api.antiek.ai/settings → Add model → add each provider
   (DeepSeek, Hermes, Xiaomi, Z_AI, Exa, SerpAPI, Browserbase, AgentMail) with
   the key from /etc/antiek/secrets.env (read via `sudo cat` on the server).
2. Verify in Settings → new Usage panel (after PR lands) that each key shows
   used/limit/balance.
3. On the server: edit /etc/antiek/secrets.env → set `ANTIEK_BYOT_ONLY=1`,
   delete the provider key lines (keep STRIPE_SECRET_KEY, CF_ACCESS_*,
   ANTIEK_AUTH_SECRET, signing keys, AGENTMAIL).
4. `sudo systemctl restart antiek.service`; check /health providers_ready.
5. Keep the env file backup (`sudo cp /etc/antiek/secrets.env /etc/antiek/secrets.env.bak-byot`).

## 5. Next-agent checklist (after this instance's PRs merge)

- [ ] Verify Aug 13 backup marker + freshness probe flip (06:30 UTC).
- [ ] Fix health probe + alert webhook (forensic report items #1–2).
- [ ] Apply apt updates + reboot; install fail2ban.
- [ ] arXiv: decide checkpointing fix vs accept partial nightly harvests.
- [ ] Run the operator-key migration (§4).
- [ ] StyleOut provenance schema addition (parent/forked_from).
- [ ] Enable Prime lane when operator wants (ANTIEK_PRIME_AGENT_RLM_ENABLED=1).
- [ ] Build slices A/B/C of the Processing spec when v1 UX is stable.
- [ ] Turbopuffer Wedge-1 spike with num_shards=2 at first write.
- [ ] Publisher ecosystem: decide marketplace (Stripe Checkout) + TollBit pilot.


---

## 6. Verification evidence (2026-08-12, all re-run by the parent)

| PR | Command | Result |
|---|---|---|
| #3033 | `pytest tests/test_prime_agent_* tests/test_prime_rpc_evidence.py tests/test_prime_exec_provider.py tests/test_rlm_*` (with pytest-asyncio) | **125 passed** in 28s |
| #3034 | `vitest run StyleWheel.test.tsx styles.test.ts` + `tsc --noEmit` | **21 passed**; tsc clean |
| #3036 | `vitest run UsagePanel.test.tsx Settings.test.tsx settingsUsage.test.ts` + tsc | **29 passed**; tsc clean |
| #3038 | `pytest tests/test_arxiv_oai_sync.py tests/tools/test_arxiv_verify.py` + live `python -m tools.arxiv_verify` | **41 passed**; live endpoint PASS |
| #3039 | `pytest tests/byok/` | **137 passed** in 12s |

## 7. Child-agent integrity notes (for the next orchestrator)

- **kimi-sub/k3 was quota-blocked** (403 billing-cycle limit) at spawn — the
  style-wheel task was re-delegated to grok-sub/grok-4.5, which delivered.
- **codex-byot-dashboard was interrupted mid-work** (worker interruption
  before its report); the parent verified the work directly (tests + tsc)
  and shipped it as #3036.
- **mimo-arxiv-verifier completed without a report**; its verifier tool +
  tests + runbook were verified by the parent and shipped as #3038. Its
  proposed `tools/arxiv_oai_sync.py` change (write high-water mark after each
  page) was **rejected as a regression** — it breaks
  `test_crash_mid_harvest_does_not_advance_high_water` +
  `test_post_crash_high_water_is_at_least_max_consumed_pre_crash` (pass on
  clean main). The existing mid-harvest cursor + `seed_max` already provide
  kill-resilience; the nightly timeout is a throughput issue, tracked in the
  arxiv runbook.


---

## ADDENDUM B — Round 2 (same goal instance, ~14:00–17:30 UTC)

### New PRs this round
| PR | Content | Verification |
|---|---|---|
| **#3045** feat/start-research-owner-model-revived | Owner-model selector for Start Research (resurrected from closed #3023): binds owner model choice to every paid role in root Loop One, one-rung BYOT dispatch, durable launch claims | 19 pytest + 41 vitest + tsc clean |
| **#3046** feat/byot-env-key-migration | BYOT env-key migration CLI (secrets.env → encrypted BYOK store; dry-run/overwrite; deletion guidance) | 66 pytest + ruff clean + mypy gate 0 |
| **#3047** feat/style-fork-provenance | Durable fork provenance (parent column + validation + legacy migration) — closes #3034's flagged gap | 59 pytest + ruff clean + mypy gate 0 |

### CI fixes applied to round-1 PRs (all pushed; re-run CI pending)
- #3033: mypy union-attr guard (missing usage on successful Prime RPC run).
- #3036: tsc -b type fixes (kind cast + null narrowing in balance chips).
- #3038: ruff strict fixes (imports, noqa E402 idiom per test_arxiv_oai_sync.py).
- #3039: ruff F401 (unused imports removed from both oauth test files).

### Server repair (prod, read-only investigation → restoration)
- **antiek-health-probe.timer re-enabled** (was inactive since 2026-05-18 21:49):
  probe verified exit 0 against live bridge + /ops/provider-ratio; timer now
  fires every 5 min; first run Result=success. Monitoring restored.

### Independent adversarial review (deepseek lineage, REVIEW.md in #3035's branch context)
- All 7 round-1 PRs: **APPROVE / APPROVE-WITH-NITS; zero MUST-FIX**. Verified:
  DuckDB single-writer preserved (new write paths use separate SQLite or
  runtime.db_lock), secret hygiene (PrimeSecret redaction, token __repr__
  redaction, encrypted-at-rest), honest states, owner scoping.
- Advisory follow-ups (low): single-source provider-credential constants (#3033),
  TODO(auth) comments + branded OAuth client registration (#3039), style-fork
  provenance event (#3034 — #3047 adds the column; event emission still open),
  ASCII ellipsis in UsagePanel (#3036).

### Ad infra audit (deepseek lineage)
- Per-second/frame-level attribution IS implemented end-to-end
  (frame_attention.py → frame_attention_accrual.py → ad_routes POST
  /api/ad/frame-telemetry → escrow via single sanctioned writer) + verification
  harness (tools/verify_ad_economics.py). Six named gaps S1–S6 from
  integration_applovin.md remain (server-minted value, IVT pre-accrual
  integration, frame→synthesis composition, one payable truth, split
  composition, auditability) — full report at /tmp/antiek-wt-ad-audit/.

### Open (next agent)
1. codex-model-picker: reusable ModelPicker for DeepResearchWorkspace +
   Notebook (in flight).
2. Merge train: 10 PRs now (#3033-#3047). Enforce-declared-bar CI needs
   re-verification after the pushed fixes (gate was red on ruff/mypy/tsc;
   parent fixed + verified locally, CI re-runs pending).
3. Operator actions unchanged: BYOT key migration (§4), health-probe webhook
   destination, apt/reboot, backup marker verification.


---

## ADDENDUM C — Round 2 CI hardening + final status (same goal instance)

### Round-2 PRs (all parent-verified)
- **#3045** owner-model binding (revived #3023) — CI fixes: mypy annotations (launch_authority_guard Iterator, install_manifest Token, _OwnerAwareCall return types), broadcast.py `_owner_model_app` typed attr, extract.py cast for with_attempt, regenerated types.ts (codegen gate), dispatch_inventory.json aligned (extract scope + synthesizer dispatch_loop_one entry), test outbound_symbols + dispatch_loop_one. Verified: 65 tests (owner API/dispatch + cost projection + codegen).
- **#3046** BYOT env-key migration CLI — CI fix: overwrite test asserts membership not list order (list_credentials is not insertion-ordered). Verified: 66 tests + ruff + mypy gate 0.
- **#3047** style fork provenance — CI clean on first run (59 tests + ruff + mypy gate 0).
- **#3048** ModelPicker + DeepResearch wiring (built inline by parent after the codex child stalled; 7 ModelPicker tests + 74 DeepResearchWorkspace tests + tsc -b clean; full suite 1945/1946 with 1 unrelated pre-existing Reading flake).

### CI status as of 18:40 UTC (verified via Actions)
| PR | CI | enforce-declared-bar | visualtest | Agent gates |
|---|---|---|---|---|
| #3033 | (re-running) | ✅ | — | ✅ |
| #3034 | ✅ | ✅ | ✅ | — |
| #3036 | ✅ | ✅ | ✅ | — |
| #3038 | ✅ | ✅ | — | — |
| #3039 | (re-running) | ✅ | — | — |
| #3045 | (re-running) | (re-running) | — | ✅ |
| #3046 | (re-running) | ✅ | — | — |
| #3047 | ✅ | ✅ | — | — |
| #3048 | (re-running) | ✅ | ✅ | — |
| #3035 | (docs-only, re-running) | ✅ | — | — |

### Lessons for the next orchestrator (CI discipline)
1. **This repo's `enforce-declared-bar` is line-keyed + content-matched**: inserting lines ABOVE baselined code can flip baselined violations NEW. Verify with the exact CI invocation: `uv pip install 'mypy==2.1.0' 'ruff==0.15.15'` then `.venv/bin/python -m tools.lints.declared_bar enforce {mypy,ruff} --baseline-file tools/lints/baselines/declared_{mypy,ruff}.json`.
2. **`tsc -b` ≠ `tsc --noEmit`** in apps/reading — run `npx tsc -b`.
3. **codegen gate**: touching `substrate/schemas/events.py` requires `python tools/codegen/emit_types.py` + commit.
4. **dispatch_inventory.json** must list every reachable dispatch entry point (new symbols need entries + test outbound_symbols update).
5. **pytest-asyncio** is required for async tests (`--with pytest-asyncio`).


---

## ADDENDUM D — Round 3: production deploy + twin-envelope backfill incident (2026-08-12 ~17:00–18:30 UTC)

### What happened
- PRs #3034/#3036/#3038 (style wheel UX, usage dashboard, arXiv verifier) were **merged by the merge train**; main tip moved to `6e4294b38`.
- The operator's documented deploy (`ansible-playbook -i inventory.ini playbooks/deploy.yml`) was executed from a **fresh main worktree** (the canonical `~/Antiek/platform` tree is still the stale deploy-source hazard — DO NOT deploy from it). The inventory is local-only (`inventory.ini` is gitignored; copy it from the canonical tree into the deploy worktree).
- **Deploy outcome**: git pull ✅, pip install ✅, SPA rsync ✅ (index-DAxamJA2.js), unit render ✅, service restart ✅ — **prod now LIVE on main tip 6e4294b38** (verified: /health build_sha, 5 providers, tunnel 200).

### The incident: schema migration hang (twin-envelope backfill)
- `python -m substrate.graph.schema` (the deploy's pre-restart migration) hung at 100% CPU for ~50 min inside `backfill_twin_source_envelopes`: **26,678 documents in prod (26,544 NULL envelopes) at ~9 rows/s**, one write transaction held the whole time. The running app error-looped on `WriteLockTimeout` (0.5s lock timeout) and burned CPU.
- Intervention: killed the schema process (write txn rolled back cleanly; column `twin_source_envelope` WAS created before the backfill, so the schema is otherwise applied), stopped the app, restarted on new code — **prod healthy again** (verified /health + journal clean). 26,544 NULLs remain (inert — new docs stamp via the normal insert path).
- **Root-cause fix shipped as PR #3049**: preload serve fields in ONE query + replicate the serve-gate decision in pure Python (same predicates/constants) + batched commits (500/batch, resumable) + metadata-only envelope on serve-gate denial (serve path still refuses). Benchmark: **30,000 rows in 22s (~1,363 rows/s, ~150x)**; drift-guard test proves byte-identical envelopes vs the per-row gate across 8 row classes; 26 tests pass; ruff + mypy gates green.
- **After #3049 merges**: the next deploy's schema step runs in seconds and clears the NULLs. Do NOT hot-patch the deployed tree.

### Current prod state (verified 18:15 UTC)
- App: new main 6e4294b38, /health 200, providers ready, zero journal errors since restart.
- SPA: fresh build (usage dashboard + style wheel + brain mascot) served at both surfaces.
- Monitoring: health probe timer active (re-enabled round 2), provider-ratio probes landing in the journal every 5 min.
- Backup: v2 script installed; next run 03:00 UTC Aug 13 (expect freshness marker flip).
- arXiv timer: active; nightly 04:20 runs (6h timeout still an open item — throughput, not state).
- Brute-force SSH attempts observed (invalid users sebastian/ubuntu/duo from 152.42.240.74 etc.) — UFW blocking; **fail2ban still not installed (operator action)**.

### PR status end of round 3
- Merged: #3034, #3036, #3038 (+ #3037 brain mascot by the fleet).
- Open + CI-verified: #3047 (enforce-bar + CI green), #3046 (enforce-bar green; CI re-run after test fix), #3033 (enforce-bar green after mypy fixes), #3048 (enforce-bar + visualtest green), #3039 (enforce-bar green), #3045 (fixes pushed; CI re-running), #3035 (docs; enforce-bar green), **#3049 (new: twin backfill fix)**.


---

## ADDENDUM E — Round 4 (same goal instance, ~18:00–21:30 UTC): Grok-led swarm + 4 new PRs + merge train

### Merged this round (fleet): #3033 (metered Prime), #3039 (OAuth), #3046 (migration CLI), #3047 (style provenance), #3045 (owner-model binding), #3048 (ModelPicker). Main tip now includes the full BYOT + Prime + style stack.

### New PRs (all parent-verified)
| PR | Content | Verification |
|---|---|---|
| **#3050** feat/arxiv-bulk-sync | **arXiv bulk-snapshot incremental sync** (grok-4.5 child): structural diagnosis (26K docs @ ~1000 rec/page @ 1-req/3.5s = 6–22h vs the 6h budget — OAI-only is impossible); `--bulk` mode streams the free ~4.5GB GCS metadata snapshot + OAI tail for deltas; stdlib-only; crash-safety preserved with new bulk-path tests | 199 arxiv tests, ruff+mypy green |
| **#3051** feat/antiek-memory-mcp-e2e | **Antiek Memory MCP complete** (mimo child): `python -m tools.antiek_memory` subprocess entry with real substrate handlers for the 4 §13.8 tools + 3 resources + prompt-injection envelope; 23-test stdio JSON-RPC wire harness | 35 tests (12 pre-existing + 23 e2e), ruff green |
| **#3052** feat/ad-server-minted-value | **Ad gap S1 closed** (deepseek child): server-minted window value from the fill/pricing record (settled-only, owner-scoped, inside the write lock); client hint re-accepted as IGNORED + audited; wire v3 with v2 compat, v1 rejected | 54 tests, ruff+mypy green |
| **#3053** feat/model-picker-write-surface | **Write-surface ModelPicker** (parent): advisory picker on the outline Generate area (writing task projection) — binding for the write/cascade runners is a documented follow-up | 76 Write tests, tsc -b clean |

### CI fixes this round
- **#3049 (twin backfill)**: 3 fix commits — import from source modules (mypy no-implicit-reexport), raw-text scanner allowlist entry with justification (internal owner-path consumer), import sort. CI re-running.
- codex child duplicated round-1 ModelPicker work (main lacked #3048 at the time); discarded — the follow-up was built directly on merged main instead.

### Operator note: Grok 4.6
The operator asked for "Grok 4.6" as the smartest swarm model. The registry exposes grok-sub/grok-4.5 (subscription) — no 4.6 selector exists; grok-sub auth recovered this round and grok-4.5 delivered the arXiv diagnosis. If a 4.6 credential appears, prefer it for review tasks.

### Open PRs at round end
#3035 (docs), #3049 (twin backfill fix — needs merge before the next deploy to avoid the 50-min migration hang), #3050, #3051, #3052, #3053.

### Next-agent priorities
1. Merge #3049, then deploy merged main (fresh worktree; inventory from canonical tree) — the twin backfill now completes in ~20s and clears the 26.5K NULLs.
2. Flip the arXiv systemd ExecStart to `incremental --bulk` after #3050 merges.
3. Bind owner-model authority into the cascade (deep-research) + write runners (the #3045 pattern extended).
4. Operator actions unchanged: BYOT key migration (§4), fail2ban, alert webhook, apt/reboot, verify backup marker after 03:00 UTC run.
