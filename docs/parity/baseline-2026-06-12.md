ALC-era record (graded caffen/ALC-integration, 2026-06-12/13) — historical baseline, not current truth; current grade comes from ALC-Revival SPR-08 re-audit.

# Parity audit — 2026-06-12
Rubric version: PostHog SHA 9c06e96956f68b6cd67e39e30df8ff736e1fcaad
Antiek tree: working tree @ 2026-06-13 (ALC SPR-03 worktree, branch `caffen/ALC-SPR-03`, git SHA `16ff381d8ee4bad6c4f858e0d696fa7ae8140643`)
Auditor context: run 1 — INDEPENDENT baseline (run A). No other baseline report read.

All `file:line` below re-resolved on the live tree under `apps/reading/`. Counts record the command/path counted from. This is the before-photo: where a surface is a 1, it is scored 1 with the evidence, not softened.

## Graded table
| Dimension | Scene | Shell | Werner | Reading-mode | Fallback | Dimension grade |
|---|---|---|---|---|---|---|
| Visual crispness | 3 | 1 | 2 | 2 | 3 | **1** |
| Motion & life | 3 | 3 | 3 | 3 | 3 | **3** |
| Product character | 2 | 2 | 2 | 1 | 3 | **1** |
| Evidence-backed craft | 2 | 2 | 2 | 2 | 3 | **2** |

**Roll-up rule used:** *lowest in-scope surface* (the AUDITOR gate is "no dimension may regress below baseline on any sprint," and a parity sprint must lift its weakest in-scope surface — an honest before-photo therefore reports the floor, not the average). Where a dimension's floor is a single named, justified exception the cell is written `2*` and the exception is named in findings; no dimension here qualified for a `2*` roll-up — the floors are genuine, not exceptions.

---

## Per-dimension findings

### Visual crispness — 1/3
The token *source of truth* is 3-caliber; the *focus-ring coverage* on the in-scope SCENE and SHELL controls is the floor, and the rubric makes that gate binding ("If the bundle exists but the scene buttons don't use it, that is a **1, not a 3**", rubric Dimension 1 coverage caveat).

- **Scene: 3/3** — `scene.css` declares motion only and adds zero hex literals: `apps/reading/src/scene/scene.css:14` ("Colours are token vars only; this file declares motion, not palette, so it adds no hex"); confirmed by count — `grep -cE '#[0-9a-fA-F]{3,6}' scene.css` = 0, `grep -nE 'border-radius:[0-9]' scene.css` = 0 raw radii. Geometry/colour for scene layers comes from the token file, not literals.
- **Shell: 1/3** — designed focus exists but is NOT systematically covered. Counted: 9 control-bearing shell components (`ls apps/reading/src/shell/*.tsx` minus test/story; `grep -cE '<button|onClick=' ` is non-zero on NavRail 13, ProductsLauncher 14, ProjectTree 10, SceneChrome 4, ThreadBreadcrumb 2, PenguinMascot 3). Only **2** carry `focus-visible:` styling: `apps/reading/src/shell/NavRail.tsx:318` (`focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ink`) and `apps/reading/src/shell/PenguinMascot.tsx:717` (`focus-visible:outline-sun`). ProductsLauncher's search input binds a **bare `:focus`, not `:focus-visible`** — fires on mouse click, the rubric's explicit level-1 symptom: `apps/reading/src/shell/ProductsLauncher.tsx:226` (`outline-none focus:border-sun`). ProjectTree (10 onClick), SceneChrome, ThreadBreadcrumb, ThreadJump, WorkflowStub carry no designed keyboard focus indicator. The shared `.feel-focusable` bundle exists (`apps/reading/src/design/feel-focus.css:9-11` `.feel-focusable:focus-visible { outline:2px solid var(--sun) }`) and is imported globally (`apps/reading/src/main.tsx:6`) but has **zero consumers** on the in-scope surfaces: `grep -rn 'feel-focusable' apps/reading/src` returns only the definition file (`feel-focus.css`) and its own test (`feel-focus.test.ts:38`) — no `className="feel-focusable"` anywhere. "The bundle exists but the scene/shell buttons don't use it" = **1**.
- **Werner: 2/3** — colours are token-sourced (`apps/reading/src/werner/ice-fishing.css:2-3` "colours via design tokens (var(--emperor) bait worm, var(--werner-bill) hook)"; `apps/reading/src/design/tokens.css:120-127` `--werner-bill/--werner-foot/--werner-rod` lock to `--sun`/`--sun-deep`). It is keyed to a designed token system, but Werner exposes no interactive `:focus-visible` control of its own (mascot, not a control) so it cannot reach the state-pair-border level-3 bar; tokenized but not control-refined = **2**.
- **Reading-mode: 2/3** — reading surfaces consume tokens (`::selection` highlighter via `var(--sun-hl-day)`/`var(--sun-hl-night)` at `apps/reading/src/index.css:22,26,30-37`, day/night swap via media query) but the reading-mode controls are not uniformly behind a `:focus-visible` token ring; tokenized + some designed focus, partial coverage = **2**.
- **Fallback: 3/3** — crispness HOLDS when the scene is off. `--glass-bg-solid` is a defined opaque fallback in BOTH themes: `apps/reading/src/design/tokens.css:111` (`--glass-bg-solid: #FBFCFD; /* opaque fallback == --card-soft */`) and `:248` (night `#1B202A`). GlassSurface renders the opaque solid (no backdrop-filter, no colour jump) under reduced-motion / scene-absent: `apps/reading/src/shell/GlassSurface.tsx:97-102,118-124`, and the documented AA ratios of the solid fallback are far above floor (`apps/reading/src/shell/GlassSurface.tsx:45` "the solid fallback itself is far above the floor (day 18:1, night 14:1)"). Token discipline confirmed by 3-level radius scale (`apps/reading/src/design/tokens.css:148-150` `--radius-sm:4px/--radius:6px/--radius-lg:10px`), 4-level shadow scale (`:142-145` `--shadow-z1..z3/--shadow-lift`), and a passing automated WCAG-AA contrast test (`apps/reading/src/design/tokens.contrast.test.ts` — ran 2026-06-13, green).

### Motion & life — 3/3
The strongest dimension. A deliberate, hand-authored system with a single source of truth, a universal reduced-motion guard, GPU-cheap keyframes, and choreographed enter AND exit on transient surfaces. The one place the rubric says to look hard — the always-on ambient living-scene — is GPU-cheap, restrained, and degrades gracefully.

- **Scene: 3/3** — 3 ambient keyframes (`apps/reading/src/scene/scene.css:16,21,27` `akb-krea-fade-in`, `akb-penguin-journey`, `akb-penguin-bob`), all GPU-cheap (transform/opacity only; `grep -nE '^\s*(width|height|top|left|margin|padding):' scene.css` inside keyframes = 0). Ambient motion is the NAMED, JUSTIFIED living-scene exception (rubric Dimension 2 logged DECISION) — not scored as a level-0 decorative violation. It freezes under reduced-motion via two agreeing guards (structural JS clock + the media query at `apps/reading/src/scene/scene.css:32-38`).
- **Shell: 3/3** — transient surfaces choreograph enter AND exit (not binary mount/unmount): `apps/reading/src/components/windows/WorkspaceWindow.tsx:260` (`exit={reduceMotion ? undefined : { scale: 0.97, opacity: 0 }}`) and `apps/reading/src/workspace/PanelLayoutPanel.tsx:147` (`exit={reduceMotion ? undefined : { scale: 0.96, opacity: 0 }}`) — paired transform+opacity exit, and reduced-motion-aware at the call site.
- **Werner: 3/3** — ~22 Werner keyframes across `apps/reading/src/werner/waddle.css`, `ice-fishing.css`, `src/brand/werner/animated/animations.css` (counted via `grep -rn '@keyframes'`); each is a named pose/feedback state (`werner-step`, `werner-hit-bump`, `werner-fishing-rod`, `werner-sleep-breath`, `werner-thinking-dot`, etc.), all transform/opacity (GPU-cheap; the only width/height/margin in `ice-fishing.css:20-22` are static `.werner-ice-bait` element setup, NOT inside the `werner-bait-bob` keyframe at `:6-14` which is transform-only). Werner pose keyframes carry their own reduced-motion home (`apps/reading/src/design/motion.css:13` note).
- **Reading-mode: 3/3** — inherits the universal guard; no rogue per-surface durations on reading controls (motion vocabulary is the named token set, below).
- **Fallback: 3/3** — motion DEGRADES GRACEFULLY, it does not vanish. Universal systemic guard (stronger than PostHog's 2-of-53 per-component): `apps/reading/src/design/motion.css:23-31` collapses ALL `transition-duration`/`animation-duration` to `0.01ms !important` under `@media (prefers-reduced-motion: reduce)`. The fallback is an AUTHORED state, asserted in a real browser: `apps/reading/e2e/glass-reduced-motion.spec.ts:33` ("the frozen scene is still painted (not a flat colour)") + `:43` ("under reduced-motion the landing text still clears WCAG-AA 4.5:1") — the scene freezes to one static legible frame, not a blank. Named motion tokens have a single source of truth: `apps/reading/tailwind.config.js:121-129` (`transitionDuration fast/base/slow = 80/150/800ms`, `transitionTimingFunction standard/enter`) sourced from `apps/reading/src/design/motion.ts` (6 importers, `grep -rln "from.*design/motion"`).

### Product character — 1/3
Per-component state authorship is genuinely strong on the chrome flagship (NavRail) but the floor is real: an in-scope reading-mode component ships a single happy-path story (the rubric's exact level-1 "default-looking surface" symptom), and the always-on scene layers carry zero Storybook authorship (mitigated, not erased, by e2e matrix coverage).

- **Scene: 2/3** — 0 co-located `*.stories.tsx` for the 6 scene rendering layers (`ls apps/reading/src/scene/layers/*.tsx` = Clouds, Peaks, Snow, ProceduralSky, KreaArtLayer, PenguinJourney; `ls apps/reading/src/scene/*.stories.tsx` = none). BUT the scene's authored states are asserted in e2e rather than Storybook: `apps/reading/e2e/feel-experience-matrix.spec.ts` + `ams-v2-experience-matrix.spec.ts` reference scene/peaks/clouds/snow/penguin 38 times (`grep -rni 'scene|peaks|...'`). The states are designed and asserted, just not Storybook-inspectable in isolation — above level-1 (not "every state is whatever the default render produces"), below level-3 (no co-located named stories per stateful layer) = **2**.
- **Shell: 2/3** — co-location 6/9 = 67%, below the ≥85% level-3 bar (`ls apps/reading/src/shell/*.stories.tsx` = NavRail, PenguinMascot, ProductsLauncher, ProjectTree, ThreadBreadcrumb, __axeNegativeControl; missing: GlassSurface, SceneChrome, ThreadJump, WorkflowStub). State authorship is uneven: NavRail authors 6 named states incl. an honest empty stub (`apps/reading/src/shell/NavRail.stories.tsx:39,52,65,78,91,104` BottomRail/FourWorkflowRail/RailWithResearchTree/RailWithReadTree/ResearchSceneChrome/HonestStub) = 3-caliber, but ProductsLauncher authors only `Open` (`apps/reading/src/shell/ProductsLauncher.stories.tsx:29`, 1 named export). ≥2 named with ≥1 non-default but below 85% co-location = **2**.
- **Werner: 2/3** — 0 co-located stories (`ls apps/reading/src/werner/*.stories.tsx` = none); Werner's poses ARE authored as named states in code (`apps/reading/src/brand/werner/animated/animations.css` pose keyframes + `src/werner/choreography.ts`) and asserted in the e2e matrix, but not as inspectable Storybook stories per pose. Designed + asserted but not story-co-located = **2**.
- **Reading-mode: 1/3** — the floor. 5 stories / 12 components (~42% co-location: `find apps/reading/src/modes/Reading -name '*.stories.tsx'` = ResearchThis, HouseSlot, AdBorder, VoiceNote, TocPanel). Two of those author exactly ONE named export each — `apps/reading/src/modes/Reading/ResearchThis.stories.tsx` (1 named) and `apps/reading/src/modes/Reading/VoiceNote.stories.tsx` (1 named) — a single default/happy-path story with no loading/empty/error/disabled state authored as its own story. That is the rubric's verbatim level-1 "default-looking surface" symptom ("exactly 1 named story for a component with ≥2 meaningful states") = **1**.
- **Fallback: 3/3** — the fallback is itself authored, not an accident: a named, asserted e2e state (`apps/reading/e2e/glass-reduced-motion.spec.ts:32` describe-block "SPR-03 — reduced-motion: frozen scene + opaque-fallback text stays legible" with two explicit assertions). The procedural floor is designed and inspectable = **3**.

### Evidence-backed craft — 2/3
Both rails are present with real substance — RAIL A is a committed Lost-Pixel image-snapshot rail (381 baselines across 127 stories × 3 viewports), and RAIL A2 is a real, CI-wired axe-core story-asserting gate — plus enforcement Antiek owns that lemon-ui lacks (motion-guard ratchet, e2e experience/resilience matrices). The dimension caps at 2 because RAIL A, though present, is weak on three countable counts — it is effectively single-theme, it EXCLUDES the flagship scene story from the check, and it is wired INFORMATIONAL/non-blocking in CI so nothing fails on pixel drift — and because RAIL B depth is shallow. The grade rests on the rail being present-but-weak, not absent.

- **Scene: 2/3** — the living scene is the surface that exposes RAIL A's weakness most sharply: its flagship story `workspace-demo--scene` is EXCLUDED from the snapshot check by `apps/reading/lostpixel.config.ts:51-54` (`filterShot` skips `workspace-demo--scene` for framer-motion spring nondeterminism), so the centerpiece is NOT pixel-gated even though stale `workspace-demo--scene__[w*px].png` baselines remain on disk. The scene IS enforced behaviorally (`apps/reading/e2e/glass-reduced-motion.spec.ts` asserts non-flat frozen frame + AA in a real browser) and against new motion sprawl (`apps/reading/src/design/motion/motion.guard.test.ts` + `motion_guard_baseline.json` baseline-ratchet on new raw `@keyframes`). Present-but-flagship-excluded visual-regression rail + a real behaviour gate = **2**.
- **Shell: 2/3** — covered by RAIL A (shell stories such as `navigation-nav-rail--default` carry committed Lost-Pixel baselines under `apps/reading/.lostpixel/baseline/`) AND by the genuine a11y gate: `apps/reading/.storybook/test-runner.ts:72-95` is a `postVisit` hook that runs axe-core (`@axe-core/playwright`) on each visited story and THROWS on any `serious`/`critical` violation (`:69` `BLOCKING_IMPACTS = new Set(["serious","critical"])`; `:84-94` throw), tag-scoped via `--includeTags a11y-audit` (`:40`). The axe gate is a real story-DOM-asserting gate but it is ACCESSIBILITY, not image regression, so per the rubric it is RAIL A2, not RAIL A. RAIL A itself is present here but non-blocking in CI (see the caps paragraph) = **2**.
- **Werner: 2/3** — Werner stories carry committed Lost-Pixel baselines (e.g. `brand-werner-animations--all-poses__[w*px].png` under `apps/reading/.lostpixel/baseline/`) and motion is ratchet-guarded by the motion guard; behaviour asserted in e2e. (Note: `apps/reading/src/brand/werner/poses/**/*.png` — 15 files incl. `poses/anchor/` — are SOURCE art, distinct from the `.lostpixel/baseline` regression snapshots.) The Werner visual-regression rail exists but shares the dimension's non-blocking/single-theme weakness = **2**.
- **Reading-mode: 2/3** — covered by the same RAIL A baselines + the a11y test-runner gate + e2e matrices; the snapshot rail is present but single-theme and non-blocking = **2**.
- **Fallback: 3/3** — the fallback is the one surface enforced on its OWN dedicated real-browser gate: `apps/reading/e2e/glass-reduced-motion.spec.ts` forces `reducedMotion:"reduce"` (`:36,45`) and asserts both the painted-not-flat frozen frame AND WCAG-AA legibility over the opaque fallback — quality ENFORCED, not asserted, on the procedural floor = **3**.

Confirming the level-3 caps (rubric requires verification by counting the mechanism): the image-snapshot rail is Lost-Pixel, not Jest/Playwright matchers (`grep -rn 'toMatchImageSnapshot|toHaveScreenshot' apps/reading/{src,e2e,.storybook}` = **0**, but that is the wrong mechanism to grep — the rail is committed baselines). Committed visual-regression snapshots under `apps/reading/.lostpixel/baseline/` = **381** (`find apps/reading/.lostpixel/baseline -name '*.png' | wc -l` = 381), spanning **127 distinct stories × 3 viewports** (1280/1024/768; the viewport is encoded as the `__[wNNNpx]` suffix). These ARE genuine visual-regression baselines and are distinct from the 15 `src/brand/werner/poses/**/*.png` source-art files (incl. `poses/anchor/`). So RAIL A is PRESENT — what caps it below 3 is its rigor: (1) **single-theme** — only one baseline carries a dark/night variant (`design-moodboard--palette-night-majestic-night-sky`), there is no systematic light+dark matrix (PostHog L3 = ≥2 themes); (2) **flagship excluded** — `apps/reading/lostpixel.config.ts:51-54` `filterShot` skips `workspace-demo--scene`, so the living-scene centerpiece is not gated; (3) **non-blocking in CI** — `.github/workflows/visualtest.yml:53-60` runs Lost-Pixel as INFORMATIONAL (`npx lost-pixel || echo "::warning..."`, explicitly until baselines are approved in the dashboard), so nothing fails on pixel drift. The level-3 gap vs PostHog's 154 paired light/dark snapshots is the theme matrix + blocking CI + flagship inclusion, not the absence of snapshots. RAIL B depth: `apps/reading/src/components/lemon/LemonButton.stories.tsx` = **4** named exports (`grep -cE '^export const '`) vs PostHog's 23 — shallower per-primitive state cross-product.

## Fairness notes (where Antiek meets or beats PostHog)
- **Motion & life:** Antiek's reduced-motion is SYSTEMIC (one universal guard, `apps/reading/src/design/motion.css:23-31`) where PostHog's is 2-of-53 per-component — Antiek beats the benchmark on reduced-motion coverage and on having named duration/easing tokens with a single source of truth (`apps/reading/tailwind.config.js:121-129` ← `src/design/motion.ts`). This is the legitimate 3/3.
- **Visual crispness (token depth):** Antiek tokenizes shadows as a 4-level scale (`apps/reading/src/design/tokens.css:142-145`) and bakes WCAG-AA into the token file with an automated test (`tokens.contrast.test.ts`, passing 2026-06-13) — PostHog's `base.scss` does neither. The scene + fallback 3/3 cells are defensible on this tree; not auto-discounted to flatter the benchmark.
- **Evidence-backed craft (enforcement breadth):** Antiek owns enforcement lemon-ui lacks — the axe a11y test-runner gate (`apps/reading/.storybook/test-runner.ts`), the motion-guard ratchet (`motion.guard.test.ts` + `motion_guard_baseline.json`), and Playwright experience/resilience matrices — and it also carries a committed Lost-Pixel image-snapshot rail (381 baselines under `apps/reading/.lostpixel/baseline/`). Antiek BEATS PostHog on enforcement breadth; on RAIL A it TRAILS on rigor (single-theme, flagship-excluded, non-blocking in CI), not on the rail's existence.
- **Product character (Fallback):** Antiek authors and ENFORCES its degraded/procedural state as a first-class named e2e experience — a discipline the rubric explicitly grades as first-class and which Antiek satisfies at 3/3.

## Honest embarrassments (the before-photo's job)
- **Visual crispness / Shell: 1/3** — a shared `.feel-focusable` focus bundle is defined and globally imported (`apps/reading/src/design/feel-focus.css:9-11`, `src/main.tsx:6`) yet has **zero className consumers** on any scene or shell control (`grep -rn 'feel-focusable' apps/reading/src` → only the def + its own test). Only 2 of ~9 control-bearing shell components carry any `focus-visible:` ring; ProductsLauncher's search input uses bare `:focus` (`ProductsLauncher.tsx:226`), which fires on mouse click — the rubric's named level-1 failure. Recorded without softening: the bundle exists, the controls bypass it.
- **Product character / Reading-mode: 1/3** — `ResearchThis.stories.tsx` and `VoiceNote.stories.tsx` (`apps/reading/src/modes/Reading/`) each ship exactly one named story (the populated happy path); no loading/empty/error state authored as its own story. This is the dimension's forbidden "default-looking surface," scored 1 on the evidence.
- **Evidence-backed craft / RAIL A present but weak: caps every on-scene/shell cell at 2** — the image-snapshot rail EXISTS (381 committed Lost-Pixel baselines under `apps/reading/.lostpixel/baseline/`, 127 stories × 3 viewports) but is non-blocking and shallow on themes: `.github/workflows/visualtest.yml:53-60` runs Lost-Pixel INFORMATIONAL (`npx lost-pixel || echo "::warning..."`) so nothing in CI fails on pixel drift; only one baseline is dark-theme; and the flagship scene story is excluded via `apps/reading/lostpixel.config.ts:51-54` `filterShot`. The only blocking-by-design visual gate is accessibility, not appearance. The single biggest level-3 gap vs the benchmark is the missing dual-theme matrix + blocking CI + flagship inclusion — recorded as-is, as a weak rail, not an absent one.
- **Product character / Scene + Werner: no Storybook authorship** — the always-on living scene (6 layers) and Werner (the mascot) carry 0 co-located `*.stories.tsx`; their states are inspectable only via e2e, never in Storybook isolation. Graded 2 (e2e-authored, not story-authored), but the Storybook gap is real and recorded.
