ALC-era record (graded caffen/ALC-integration, 2026-06-12/13) — historical baseline, not current truth; current grade comes from ALC-Revival SPR-08 re-audit.

# Parity audit — 2026-06-13
Rubric version: PostHog SHA 9c06e96956f68b6cd67e39e30df8ff736e1fcaad
Antiek tree: working tree @ 2026-06-13 (ALC SPR-08 worktree, branch `caffen/ALC-SPR-08`, off integration `db0f8556`)
Auditor context: SPR-08 gate run — scoped to **Visual crispness / Shell** (the SPR-08-owned dimension·surface). Other cells re-confirmed against the 2026-06-12 baseline for the no-regression check; they are graded here only to the depth needed to prove no regression.

This audit grades WHAT IS, with evidence. No fixes proposed (per AUDITOR.md "you are a judge"). Every line number re-resolved on the live tree.

## Graded table
| Dimension | Scene | Shell | Werner | Reading-mode | Fallback | Dimension grade |
|---|---|---|---|---|---|---|
| Visual crispness | 3 | **3** | 2 | 2 | 3 | **2** |
| Motion & life | 3 | 3 | 3 | 3 | 3 | **3** |
| Product character | 2 | 2 | 2 | 1 | 3 | **1** |
| Evidence-backed craft | 2 | 2 | 2 | 2 | 3 | **2** |

**Roll-up rule:** *lowest in-scope surface* (the AUDITOR no-regression gate operates per surface; an honest roll-up reports the floor). The SPR-08-owned cell is **Visual crispness / Shell**, which moved **1 → 3**. The Visual-crispness DIMENSION roll-up is **2** (its floor is now Werner 2 / Reading-mode 2 — neither is SPR-08's surface), up from the baseline dimension grade of **1** (whose floor WAS the shell 1). So the SPR-08 gate is met two ways: the owned surface reached 3, AND the dimension floor rose 1 → 2 because the shell is no longer the floor.

## Per-dimension findings

### Visual crispness — 2/3 (dimension); **Shell 3/3 (the SPR-08 surface, up from 1)**

- **Shell: 3/3 (was 1/3).** The baseline embarrassment is closed. The rubric's level-3 bar is (a) token-sourced radius/border/shadow via `var()`, (b) borders/shadows/focus from COLOUR TOKENS not literals, (c) a designed `:focus-visible` ring keyed per-control, (d) a sourced disabled opacity. Evidence on the live tree:
  - **Designed focus, full coverage (the 1→3 lift).** The shared `.feel-focusable` bundle had **ZERO** shell consumers at baseline; it now has **5** control-bearing shell components consuming it (`grep -rln feel-focusable src/shell` → `NavRail.tsx`, `ProductsLauncher.tsx`, `SceneChrome.tsx`, `ThreadBreadcrumb.tsx`, `ProjectTree.tsx`), plus 2 controls with a deliberate explicit `:focus-visible` (`NavRail.tsx:318` home igloo `focus-visible:outline-ink`; `PenguinMascot.tsx:784` `focus-visible:outline-sun`). **21/21 interactive shell controls carry a focus-visible ring** (enumerated in `apps/reading/docs/shell-state-inventory.md`).
  - **Bare `:focus` eliminated.** The rubric's named level-1 symptom — ProductsLauncher's search input `outline-none focus:border-sun` (baseline `ProductsLauncher.tsx:226`) — is gone: it is now `feel-focusable … focus-visible:border-sun` (`ProductsLauncher.tsx:~226`). `grep -rn "focus:" src/shell | grep -v focus-visible` = **0**.
  - **The ring is designed for the scene.** `design/feel-focus.css` is now a DUAL-TONE ring (`--sun` core + a fixed-dark `--feel-focus-halo`, `tokens.css`), proven to clear the WCAG 1.4.11 3:1 non-text floor over the four worst scene/chrome cells (black / white / light-card / dark-card) in `src/design/feel-focus.test.ts` (best-of-{sun,halo} ≥ 12:1 every cell). A single-tone sun ring failed over light surfaces (1.3:1); the dual tone fixes it without dimming the scene.
  - **State-differentiated borders.** Scene-tab current vs rest is a token border PAIR (`SceneChrome.tsx:~223` `border-sun text-ink` current ↔ `border-transparent text-shadow-1` rest, `aria-current`); rail active uses an accent slab + `aria-current`.
  - **Sourced disabled.** `SceneChrome.tsx:~198` action verbs `disabled:opacity-60 disabled:cursor-not-allowed` (async-busy guard); `ProductsLauncher` unbuilt modes `disabled={!m.built}` → opacity-70 + "not yet" tag (an honest, sourced disabled).
  - **Geometry/colour token-sourced + `var()`-consumed.** The four grep gates are clean on `src/shell/`+`src/scene/`: 0 raw `z-index`/`zIndex` literals (all via `src/shell/zScale.ts` tokens), 0 raw rgba/hex colour literals, 0 raw durations. Radius via `rounded`/`rounded-hog`/`rounded-lg` (tokenised Tailwind) + `var(--radius-sm)` (badge); shadow via `shadow-z*` (tokens.css `--shadow-z1..z3`); glass via `bg-glass`/`border-glass`/`backdrop-blur-glass` (tokens.css `--glass-*`).
  - **One named, justified deliberate distinction (not a defect):** ProductsLauncher + CommandPalette form a "launcher overlay" idiom (`rounded-lg`/`shadow-2xl`/`border-rule`) deliberately calmer than the chunky `LemonModal` idiom (`rounded-hog-lg`/`shadow-lift`); the two launchers AGREE with each other (recorded in the SPR-08 handoff unify-vs-deliberate table). These are tokenised Tailwind utilities, not raw literals.
- **Scene: 3/3 (unchanged).** `scene.css` declares motion only, 0 hex; the Scene root `z-0` migrated to the `zScale.scene` token (`Scene.tsx:99` inline `zIndex: zIndex.scene`) — still 0, byte-stable rendering. Token discipline intact.
- **Werner: 2/3 (unchanged).** Colours token-sourced (`werner/ice-fishing.css`, `tokens.css` `--werner-*`); the mascot exposes a designed `:focus-visible` control (`PenguinMascot.tsx:784`) but as a mascot, not a state-pair-border control — tokenised, not control-refined = 2. (The fishing-cursor z migrated to `zScale.werner`; no crispness change.)
- **Reading-mode: 2/3 (unchanged).** Out of SPR-08 scope; reading surfaces consume tokens but not uniformly behind a focus ring = 2 (baseline).
- **Fallback: 3/3 (improved robustness).** Crispness holds scene-off via `--glass-bg-solid` (`tokens.css`). NEW: the operator SceneStatusBadge night-contrast (SPR-05 deferral a) is closed — it now paints on the opaque `--badge-bg` with a dedicated `--badge-text` clearing AA-normal on both themes (day 6.38:1, night 6.95:1), replacing the prior `--text-muted`-over-`--glass-bg` ≈3.79:1 (`scene/SceneStatusBadge.tsx`, `tokens.css --badge-*`). Still a sourced, opaque, legible fallback = 3.

### Motion & life — 3/3 (no regression)
- **Shell: 3/3.** Transient surfaces still choreograph enter+exit (`WorkspaceWindow.tsx:258,260` initial/animate/exit, reduced-motion-aware). SPR-08 ADDED token transitions on the shell control hover/colour states (`transition-colors duration-base ease-standard`, 18× in `src/shell`); 0 raw durations (grep gate clean). The universal reduced-motion guard (`design/motion.css:23-31`) is untouched.
- **Scene/Werner/Reading-mode/Fallback: 3/3.** Untouched by SPR-08 (the z-index migration is paint-order only; the SPR-07 Werner scene-mood/moment wiring is intact — `ams-v2-resilience-matrix` z-restack-under-reduced-motion e2e passes).

### Product character — 1/3 (no regression; OUT of SPR-08 scope)
- Unchanged from baseline. SPR-08 ADDED `apps/reading/docs/shell-state-inventory.md` (the control×state authorship doc) and a focus-state e2e (`shell-focus-states.spec.ts`), which strengthen shell state-authorship evidence, but the dimension floor is **Reading-mode 1/3** (`ResearchThis.stories.tsx` / `VoiceNote.stories.tsx` each 1 named story) — outside SPR-08's surface, unchanged. Shell stays 2/3 (co-location 6/9 < 85%). No regression.

### Evidence-backed craft — 2/3 (no regression)
- Unchanged. RAIL A (Lost-Pixel) still present-but-weak (single-theme, flagship-excluded, non-blocking). SPR-08 ADDED enforcement: `src/shell/zScale.test.ts` (the F-1/F-2 band-disjointness assertion), `feel-focus.test.ts` (focus coverage + dual-tone ring contrast over 4 cells), `e2e/shell-stacking.spec.ts` (the F-1 RED-then-GREEN browser regression), `e2e/shell-focus-states.spec.ts` (focus-ring renders in a real browser). These RAISE shell enforcement breadth but the dimension cap (RAIL A theme/blocking/flagship rigor) is unchanged = 2. No regression.

## Fairness notes (where Antiek meets or beats PostHog)
- **Visual crispness / Shell focus ring:** Antiek's dual-tone ring is contrast-PROVEN over the worst scene/chrome cells with an automated test (`feel-focus.test.ts`) — PostHog's `base.scss:1138` global `:focus-visible` ring is a single `outline: 2px var(--color-accent)` with no over-scene contrast proof (it has no always-on living scene to defend against). Antiek's per-control coverage + the contrast guard meets and, on the scene-legibility sub-criterion, exceeds the benchmark's needs.
- **Visual crispness / z-discipline:** Antiek now has a SINGLE documented z-scale with a token-file ASSERTION that no two families share a band (`zScale.ts assertNoSharedBands` + `zScale.test.ts`). PostHog scatters z-indices across SCSS with no such invariant. Antiek beats the benchmark on stacking discipline.

## Honest embarrassments (recorded without softening)
- **Build-storybook is RED — but PRE-EXISTING and NOT SPR-08's.** `npm run build-storybook` fails at the csf static-indexer on `src/brand/WernerScene.stories.tsx` ("Unexpected token, expected ',' (77:3)"). VERIFIED pre-existing: the file was committed by SPR-07 (`git log` → `db0f8556`), is byte-identical in the working tree, and the build fails with ALL SPR-08 changes stashed. SPR-08's CSS/token changes pass the PostCSS/Tailwind build (`npm run build` ✓) and the storybook PREVIEW transform (62 modules ✓); only the JS-indexer step on that one SPR-07 file is broken. Recorded as an inherited blocker, not an SPR-08 regression (see handoff).
- **Product character / Reading-mode 1/3 persists.** Outside SPR-08's surface; the dimension floor is unchanged. Recorded.
- **RAIL A still non-blocking + single-theme + flagship-excluded.** SPR-08 added shell enforcement but did not change the Lost-Pixel rail's rigor (out of scope). Recorded.

## Gate verdict (citing AUDITOR.md DOWNSTREAM GATE LANGUAGE)
> **SPR-08** must reach its target on **visual crispness (shell)**. "≥ target" = grade 3, OR grade 2 with a single named justified exception. No dimension may regress below its 2026-06-12 baseline.

- **Visual crispness / Shell: 3** — **target met** (≥3, and > baseline 1).
- **No dimension regressed below the 2026-06-12 baseline:** Visual crispness dimension 1→**2**; Motion 3→3; Product character 1→1; Evidence-backed craft 2→2. **No regression.**
- **VERDICT: SPR-08 parity gate PASSES.**
