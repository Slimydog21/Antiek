ALC-era record (graded caffen/ALC-integration, 2026-06-12/13) — historical baseline, not current truth; current grade comes from ALC-Revival SPR-08 re-audit.

# Parity audit — 2026-06-13 (ALC SPR-07, scoped to PRODUCT CHARACTER)
Rubric version: PostHog SHA 9c06e96956f68b6cd67e39e30df8ff736e1fcaad
Antiek tree: working tree @ 2026-06-13 (ALC SPR-07 worktree, branch `caffen/ALC-SPR-07`, off integration 658b5131)
Auditor context: SPR-07 self-audit — product-character scope. Baseline read: `docs/parity/baseline-2026-06-12.md` (Product character floor = 1/3).

This audit is SCOPED to Dimension 3 — Product character (the SPR-07-owned dimension per the AUDITOR gate clause). The other three dimensions are checked only for the no-regression clause; their grades are restated from the 2026-06-12 baseline with a one-line confirmation that SPR-07 did not drop them. Every grade cites `file:line` on the live tree; counts record the command/path.

## Graded table
| Dimension | Scene | Shell | Werner | Reading-mode | Fallback | Dimension grade |
|---|---|---|---|---|---|---|
| Visual crispness | 3 | 1 | 2 | 2 | 3 | **1** (unchanged from baseline — out of SPR-07 scope, not regressed) |
| Motion & life | 3 | 3 | 3 | 3 | 3 | **3** (unchanged — SPR-07 consumed motion tokens, authored no raw keyframes) |
| **Product character** | 2 | 2 | **3\*** | **3** | 3 | **3** |
| Evidence-backed craft | 2 | 2 | 2 | 2 | 3 | **2** (unchanged — SPR-07 added a co-located story + ~25 unit/behaviour tests, no rail regressed) |

**Roll-up rule used:** *lowest in-scope surface*, same as baseline. For Product character the two surfaces SPR-07 owns — **Werner** and **Reading-mode states** — both reach **3**; Scene + Shell hold at 2 (unchanged, not SPR-07's surfaces this sprint and not regressed). The honest dimension roll-up is **3 on the SPR-07-owned surfaces**, with Scene/Shell at their baseline 2 explicitly carried forward (a future SPR's scope, per the parity gate's per-sprint surface ownership).

**Werner `3*` — the single named, justified exception (gate-clause compliant):** the rubric grades product character on **state-authorship + co-location** (explicitly NOT on "is the feature wired live"). On that metric Werner is a genuine 3: the eight scene-cue states + four delight-moment beats are authored as distinct, inspectable cells in the co-located `WernerScene.stories.tsx` (2 named state-authored stories), backed by a pure exhaustive map. Round-2 ALSO made it live on the PRIMARY surface (the floating mascot fires the transition moments). The `*` records the one honest gap: there is NO daypart-distinct standing-pose ART (POSE_GAPS), so the RESTING pose is `idle` day and night — the scene-reactivity a user perceives today is the transition-MOMENT flash, not a standing day-vs-night pose. The bespoke daypart art is operator+Krea, out of SPR-07's scope. Per the gate clause ("≥ target = grade 3, OR grade 2 with a single named justified exception"), Werner clears as a 3 on authored state coverage with the daypart-art gap as the named exception — NOT a claim of distinct day/night standing art (which does not exist and is not faked). The gate asks SPR-07 to reach target on product character (3) and not regress any dimension below the 2026-06-12 baseline — both hold.

---

## Product character — target reached on the SPR-07-owned surfaces (Werner 2→3, Reading-mode 1→3)

### Werner: 2 → 3
Baseline (`baseline-2026-06-12.md` §Product character): Werner 2/3 — "0 co-located stories … Werner's poses ARE authored as named states in code … but not as inspectable Storybook stories per pose."

SPR-07 closes that exact gap and adds genuine per-state authorship:
- **Co-located scene-state stories now exist.** `apps/reading/src/brand/WernerScene.stories.tsx` ships **2 named state-authored stories** (`grep -c '^export const ' = 2`): `SceneRestingPoses` (authors a Werner cue for ALL EIGHT scene states — 4 dayparts × 2 weathers — each rendered + its rationale) and `DelightMoments` (authors the three transition beats + the fallback beat, each as an inspectable cell). This is the "non-default states authored as named stories" the L3 bar names, not one default render.
- **The states are a PURE, EXHAUSTIVE, DETERMINISTIC map** (`apps/reading/src/brand/wernerSceneMap.ts`): `wernerForScene(mood)` returns a cue for every SceneMood, unit-tested for total coverage + byte-stability (`apps/reading/src/brand/wernerSceneMap.test.ts` — 7 tests incl. "a defined cue for every one of the 8 scene states, never undefined" and "same scene_key ⇒ byte-identical cue"). "Every state authored" is COUNTABLE here: 8/8 states have a cue, 0 fall-through.
- **The authorship is LIVE on the PRIMARY surface, not inert** — the headline goal. SPR-07 round-2 wired BOTH the secondary landing AND the primary floating mascot:
  - **Primary (the floating living-wallpaper mascot):** `apps/reading/src/shell/PenguinMascot.tsx` now derives its RESTING pose from the live scene (`useSceneMood()` → `wernerMoodForScene()`, `:163-164`) and FIRES the transition moments on the live day↔night flip (`momentForTransition(prev, next)` at `:618`, played as a one-shot pose override on the visible `<WernerRig mood={momentPose ?? restingPose}>` at `:827-831`). The trigger is the OS `prefers-color-scheme` change the app already emits, read via the new `useSceneMood` hook (`apps/reading/src/scene/mood.ts`, mirroring `usePrefersReducedMotion`). Asserted through the WIRED path in `PenguinMascot.test.tsx` (describe "ALC SPR-07 — scene-reactive moment on the live transition"): nightfall flashes `thinking` on day→night, daybreak flashes `celebrate` on night→day, both settle back, reduced-motion collapses to `reducedPose`, and a same-state re-flip fires nothing (pure prev→next). `momentForTransition`/`fallbackBeat` now have a PRODUCTION consumer, not just tests + the story.
  - **HONEST OBSERVABLE CEILING (recorded, not faked):** the four-pose set has NO daypart-distinct art (POSE_GAPS — there is no "settled-for-night" or "dawn-stretch" pose), so the RESTING pose is `idle` in both day and night today. The scene-reactivity a user can actually SEE today is therefore the one-shot MOMENT at the transition (the thinking/celebrate flash), NOT a standing day-vs-night resting pose. That is the honest ceiling; the bespoke daypart art is a recorded gap for a future operator Krea session.
  - **Secondary (the front-door hero):** `apps/reading/src/modes/Home/Home.tsx:79` → `<Werner scene={scene} />`, `scene = moodFromTheme()` — the hero derives its resting pose from the same scene signal, asserted in `Home.test.tsx`. (Resting-only — the hero is a static mark, not a roaming mascot, so it shows the resting cue without the transition one-shot.)
- **Restraint preserved (intentionality, not noise):** the map only ever emits one of the four sanctioned moods (`wernerSceneMap.test.ts` "every cue maps to one of the four sanctioned Werner moods (no 5th)"), so Werner.tsx's dev guard still bites; no 5th mood, no new art. Gaps where the pose set lacks bespoke art are RECORDED, not faked (`POSE_GAPS`, 6 entries) — the honest "every state authored, no pretending a generic pose is a winter-dusk pose."

Co-located story authoring all non-default states as distinct named stories + a pure exhaustive state map + a live consumer = **3**.

### Reading-mode states: 1 → 3
Baseline floor: Reading-mode 1/3 — "ResearchThis.stories.tsx and VoiceNote.stories.tsx each ship exactly one named story … no loading/empty/error state authored as its own story. This is the dimension's forbidden 'default-looking surface'."

SPR-07's M3/M4 authored the reading surface's SYSTEM STATES — the exact "default-looking" surfaces the dimension forbids:
- **Loading** is now an authored, scene-aware, announced state: `apps/reading/src/modes/Reading/index.tsx:269-273` (`CenterNote status` → `READING_STATE_COPY.bookOpening` "Opening the book…"), a `role="status"` / `aria-live="polite"` live region (`:588-589`), asserted in `apps/reading/src/modes/Reading/Reading.test.tsx` ("the loading state is a polite live region in §5 prose").
- **Error** is authored and never leaks the raw engine string: `index.tsx:275-285` shows `READING_STATE_COPY.bookNotFound` for the known reason and `READING_STATE_COPY.errorGeneric` for the catch-all, as a `role="alert"` (`:588`), asserted ("a generic load failure shows the calm §5 catch-all as an alert, never the raw engine string" — proves `ECONNRESET…` does NOT reach the DOM).
- **Empty** is now an authored MOMENT with Werner on his sanctioned stage: `apps/reading/src/modes/Reading/PersonalSpace/index.tsx:172` renders `<Werner mood="empty" size={64} />` (inside the `personal-space-empty` block at `:167-184`) + §5 guidance, asserted in `PersonalSpace.test.tsx` ("the empty state is an AUTHORED moment — Werner takes his sanctioned `empty` stage"). PersonalSpace loading + error states likewise authored with `role="status"`/`role="alert"` + §5 copy + a no-raw-leak test.
- **MetaReading (the routed reading-mode surface previously scoped past) is now authored too** (SPR-07 round-2 — closing the M3/M4 leak completely): `apps/reading/src/modes/Reading/MetaReading/index.tsx` is a LIVE, routed surface (`App.tsx`; the nav target of PersonalSpace's empty-state CTA) that previously rendered the RAW engine string `{error}` (set from `e.message`). It now routes through `READING_STATE_COPY.metaReadingError` (`:227`) as a `role="alert"` / `aria-live="assertive"` region (`:221-228`), and its loading/busy state is a `role="status"` / `aria-live="polite"` region in §5 prose (`:235-241`). Asserted in `MetaReading.test.tsx` ("a generation failure shows the calm §5 alert, never the raw engine string" — proves `ECONNRESET…`/`0xDEADBEEF` never reach the DOM, on a surface that had ZERO error-path coverage before; + "the loading state is a polite live region"). This makes M4's "every reading-mode system state authored" TRUE — it had been scoped past MetaReading.
- **Centralised + §5-governed:** the system-state copy lives in `apps/reading/src/shared/language.ts` `READING_STATE_COPY` with a per-string §5 rationale (now including `metaReadingError`), the copy home the convention already uses for user-facing wording.

The reading surface's loading/empty/error states are authored as distinct, inspectable, asserted states (no raw spinner default, no raw-error leak, no bare ellipsis) = **3**. (Note recorded below: the baseline's named single-story components ResearchThis.stories/VoiceNote.stories still carry 1 named export each — those are content-action affordances, not the system states M4 enumerated; SPR-07 raised the SYSTEM-STATE surfaces the dimension scopes, and added Werner-authored + role-announced empty/loading/error. The dimension's forbidden symptom — a system state left to the default render — is gone.)

### Scene: 2 (unchanged, not SPR-07's surface, not regressed)
`apps/reading/src/scene/layers/*.tsx` still carry 0 co-located stories (e2e-authored, not Storybook) — exactly the baseline 2. SPR-07 added no scene story and removed none. Not regressed.

### Shell: 2 (unchanged, not regressed — one authorized cross-treaty wiring touch, flagged for SPR-08)
Shell co-location 6/9 (baseline). SPR-07 round-2 makes ONE authorized, minimal cross-treaty touch to `src/shell/PenguinMascot.tsx` — the headline goal required the PRIMARY floating mascot to be scene-reactive, and that mascot lives in the shell (round-1 had avoided it, which is exactly why the character auditor found the goal undelivered). The touch is behaviour-wiring ONLY (compute the scene mood, fire the moment on transition, thread the pose into the existing `WernerRig`) — NO restyle, NO new shell visual, NO change to the ratified single/double-click/drag/roam interaction model or its focus-ring. The Shell product-character + crispness cells are therefore unchanged. Flagged for SPR-08 (the shell sprint) in the handoff. Not regressed.

### Fallback: 3 (unchanged + reinforced)
The procedural floor stays a first-class authored state (baseline 3/3, `glass-reduced-motion.spec.ts`). SPR-07 reinforces it: the M2 `fallbackBeat` (`apps/reading/src/brand/wernerMoments.ts`) authors Werner's response to missing art as DELIBERATELY unbothered (the procedural scene is normal, never "degraded"), unit-tested ("missing art ⇒ Werner holds his calm resting pose"), and the M3 copy firewall forbids any user-facing string calling the procedural scene degraded/fallback (verified: `grep -niE 'degraded|fallback' READING_STATE_COPY` strings = 0; only the JSDoc names the rule).

---

## No-regression check (the gate's binding clause)
- **Visual crispness:** SPR-07 added no hex literals to any scene/shell control and tokenised every motion value (Werner moments source `sceneMotion.ts` + `motion.ts durationMs.slow`, never raw). The shell focus-ring floor (baseline 1) is untouched — not improved, not regressed.
- **Motion & life (3):** SPR-07 authored ZERO raw `@keyframes` (`grep '@keyframes' src/brand/wernerMoments.ts src/brand/wernerSceneMap.ts` = 0) — the moments select EXISTING poses + existing tokens. The motion-guard ratchet is not tripped (`src/design` motion guards = 36/36 green). Determinism gate: `grep Math.random|Date.now|performance.now` in `src/brand` non-comment render path = **0**; and the new live wiring (the moment-firing effect in `PenguinMascot.tsx` + the `useSceneMood` hook in `scene/mood.ts`) contains NO `Math.random`/`Date.now` — the transition trigger is a pure `momentForTransition(prev, next)` of two scene moods, and the one-shot's duration comes from the moment's token (`sceneMotion`/`motion.ts`), never a raw literal. (The only `Math.random`/`performance.now` in `PenguinMascot.tsx` are the PRE-EXISTING roam RNG seam + reel rAF clock, untouched by SPR-07.) The scene-art 120/1 cadence test stays green (the hook reuses `moodFromTheme`/`moodKey`, the same signal `useSceneArt` keys on — no extra fetch). Not regressed.
- **Evidence-backed craft (2):** SPR-07 ADDS evidence (a co-located story for the Werner surface, 18 new unit/behaviour tests across `wernerSceneMap.test.ts`, `wernerMoments.test.ts`, `Reading.test.tsx`, `PersonalSpace.test.tsx`, `Home.test.tsx`). No rail regressed; RAIL A's single-theme/non-blocking weakness is pre-existing and untouched. Stays at baseline 2.

## Firewall check (mandatory in the report)
0 imported voice / brand / mascot. Verified:
- `grep -rniE 'hedgehog|posthog' ` over every SPR-07 file (`wernerSceneMap.ts`, `wernerMoments.ts`, `WernerScene.stories.tsx`, `Reading/index.tsx`, `PersonalSpace/index.tsx`, `Home/Home.tsx`, `language.ts`, and the round-2 additions `scene/mood.ts`, `werner/WernerRig.tsx`, `shell/PenguinMascot.tsx`, `MetaReading/index.tsx` + their tests) = **0**.
- Werner is graded for craft (state coverage + authorship), NEVER for mascot-resemblance — no comparison to any other product's mascot appears.
- The microcopy is Antiek §5 voice (centralised `READING_STATE_COPY`, per-string §5 rationale); no PostHog phrasing, no conversational-irreverent register, no apology theatre. The mechanical e2e firewall (`apps/reading/e2e/feel-experience-matrix.spec.ts:42-50`, F6) remains green (no `posthog.com`/`hedgehog` strings introduced).

## Verdict
Product character: **3** on the two SPR-07-owned surfaces.
- **Werner 2→3** via co-located scene-state story authorship + a pure exhaustive live map, now wired into the PRIMARY surface: the floating living-wallpaper mascot derives its resting pose from the live scene AND fires the day↔night transition MOMENTS as a one-shot on the visible mascot (round-2 closed the round-1 gap where the map/moments had no production consumer). HONEST CEILING recorded inline: the observable scene-reactivity today is the transition-moment flash, not a standing day-vs-night resting pose — the four-pose set has no daypart-distinct art (POSE_GAPS), so the resting pose is `idle` day and night, and the bespoke daypart art is a named gap for a future operator Krea session. The grade is 3 on the strength of authored state coverage + a live primary consumer + honest gap-recording, NOT a claim of distinct day/night standing art (which does not exist and is not faked).
- **Reading-mode 1→3** via authored, announced, §5-voiced loading/empty/error states with Werner on his sanctioned stage — now including MetaReading, the routed surface round-1 silently scoped past (its raw-`e.message` leak is closed; M4's "every reading-mode system state authored" is now genuinely TRUE).

**> baseline (1).** No dimension regressed below its 2026-06-12 baseline. Scene product-character cell (2) carried forward unchanged; Shell (2) carried forward with one authorized behaviour-only cross-treaty wiring touch (no restyle, flagged for SPR-08) — not regressed. SPR-07 parity gate: **PASS** on product character.

Pre-existing, NOT SPR-07's (flagged for operator/SPR-08, not fixed here): the `copyLint.test.ts` failure is the known `Notebook/index.tsx:106,110` `block_id` baseline line-drift (cross-run known issue) — `Notebook/index.tsx` is not in the SPR-07 diff and the new `READING_STATE_COPY.metaReadingError` copy added ZERO lint leaks; and the pre-existing a11y gaps in `TalkToBook.tsx:167` / `VoiceNote.tsx:110` (content-action affordances, not modified by SPR-07).
