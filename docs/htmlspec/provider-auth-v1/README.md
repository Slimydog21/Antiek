# Antiek v1 provider authentication

Self-contained execution specification for owner-scoped provider API keys, a future official-client local harness, and provider-approved web OAuth. Open `index.html`; each sprint page is cold-executable and includes its own dependencies, exclusions, hostile gates, and harness hint.
