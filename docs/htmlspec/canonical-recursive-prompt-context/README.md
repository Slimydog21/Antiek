# Canonical recursive prompt context

Correction spec for PR #913’s counts-only session pack. It defines canonical bounded content resolution, real prompt-consumer integration, and privacy-preserving advisory outcome feedback. Start with `index.html`.
