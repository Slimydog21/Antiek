# Midnight Oil graph-admission retry

One offline sprint closes the gap between Midnight Oil's truthful `pending` admission state and its existing provider-free projection-resume primitive: an authenticated operator action retries only graph effects, never research dispatch or spend.
