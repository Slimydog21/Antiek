# ANT-DRL — Perfect Deep Research Loop

HTML master spec + nine sprint pages converging DRW (Cascade) gather with Loop 1 (Ask) synthesis under a single `DeepResearchComplete` terminal contract. Architecture fork **A (converge)** ratified at spec time. **Waves 1–6 shipped** (2026-06-12). **Wave 7 (SPR-DRL-09)** gates operator dogfood with pack fidelity + parent-terminal observability (Cycle-2 tribunal).

**Open:** `docs/htmlspec/deep-research-loop/index.html` in a browser (offline-safe).

**Execute next:** `sprint-09-dogfood-readiness.html` via `/caffenagent-cycle --from-phase exec-2` or `/caffenagent`.

**Verify profile:** `./scripts/canonical_verify.sh deep-research` (P-11..P-16 today; P-17 added in SPR-DRL-09).

Rigor audit rung: 3 (single-context movability pass by orchestrator).