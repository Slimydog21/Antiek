# ResearchArtifact account hosting

Execution-ready HTML master spec and three serial sprint manuals for migrating ResearchArtifact from globally keyed local files to authenticated account-scoped private assets. Start with `index.html`; implementation is intentionally deferred until the active #866/#873 source ownership is released.
