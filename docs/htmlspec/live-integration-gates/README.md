# Live integration gates

Canonical, HTML-native execution contracts for Antiek's remaining L1–L6 live boundaries and the
L7 NotDiamond authority rejection. Start with `index.html`; generated campaign artifacts remain
forbidden.

These files are repository-native instructions for executing agents, not application routes. User
guidance is intentionally mapped through
`apps/reading/src/workspace/capabilityGuidanceLinks.ts` to served, honest Settings status panels.

Regenerate deterministically with:

```sh
python3 docs/htmlspec/live-integration-gates/_generate.py
```

Operator-only network, provider, payment, deployment, and spend checks remain `NOT RUN` until the
named sprint dependencies and explicit authority are present.
