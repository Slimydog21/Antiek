# Research Route Instrument

Cycle 591 operating spec: turn the research composer’s real Fast/Deep routing control into an honest per-prompt decision instrument. The browser may select only server-issued route IDs; launch re-resolves the ID to the closed research-tier map. The instrument exposes readiness, model/thinking policy, and budget projection without changing the independently pinned synthesis route.

- Master: `index.html`
- Sprint: `sprints/SPR-01-route-contract-and-instrument.html`

