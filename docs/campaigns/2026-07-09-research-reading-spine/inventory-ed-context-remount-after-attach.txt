# residual (ed) remount research context after publication attach
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(ec) prior | branch_only | campaign inventories
(ed) context remount after attach | branch_only | PublicationAttachPanel onAttached; DR host remounts context; 21 vitest

## What shipped
- PublicationAttachPanel `onAttached` after HTML attach+hydrate.
- DeepResearchSessionHost wires onAttached → contextRefreshKey bump (same path as twin promote).
- HTML-first hydrate invariant held.

## Why
arxiv/substack/URL attaches must appear in research context pack + citation trust without stale pre-attach mount.

NEXT: (ee) re-derive; operator merge PR #465
