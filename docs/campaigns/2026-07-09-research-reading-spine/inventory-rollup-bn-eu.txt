# Campaign rollup (bn)–(eu) · PR #465
Hosted collective multi-select (eu); full/float DR reading+hosted; draft auto-open; twin seed; driver budget bar.
Operator merge still gated. Next residual (ev).
