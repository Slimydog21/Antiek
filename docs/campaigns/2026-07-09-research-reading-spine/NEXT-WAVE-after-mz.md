# Next-wave residual map after (mz) · PR #465

## Closed (my→mz)
Chase multi-selected twin notes as floating|full deep research via
`launchFloatingDeepResearch` chokepoint. `buildTwinChasePayload` orders
questions before insights; selection clears on success.

## Next
1. Operator merge PR #465 (highest leverage)
2. Budget soft-gate on twin chase launch (parity marketplace iy)
3. Collective multi-select merge of chase spawns (existing collective panel)
4. SESSION-ARC mq–mz twins promote/chase wave handoff
5. Live L1–L4 dual-gate remains operator-only

**(na)+** re-derive. Infinite continues until STOP.
