# Next-wave residual map after (ol) · PR #465

## Closed (ok→ol)
Collective auto-selects newest recent_ring spawn when selection is empty and
preferredSpawnId is unset (autoSelectNewestRecent default true). Does not
re-auto-select after operator clears the same newest.

## Next
1. Operator merge PR #465 (highest leverage)
2. Continue dogfood product polish
3. Live L1–L4 dual-gate remains operator-only

**(om)+** re-derive. Infinite continues until STOP.
