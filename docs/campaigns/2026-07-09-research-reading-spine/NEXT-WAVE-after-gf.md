# Next-wave residual map after (gf) · PR #465

Write piece engagement parity with hosted HTML host is largely closed on branch:
twins, context, driver badge, HTML re-import, deep research + pub refs,
collective multi-select, twins remount key (gg).

## Closed on branch (Write land + engagement)

fl–gg Write HTML land + open-piece engagement surfaces. See inventories
`inventory-fl-*` … `inventory-gf-*` and tip via `git log -1`.

## Operator / env gated (do not fake green)

1. PR #465 main merge — operator only  
2. Live multi-agent collective chat council  
3. Live Midnight Oil multi-provider  
4. Live hydrate / twin seed injectors  
5. Paid marketplace rails  
6. NotDiamond **dispatch** (advisory only)  
7. Antiek-bench weekly LaunchAgent install  
8. Live arxiv/substack network hydrate  

## Highest-value unblocked product residuals

1. **Write DR budget projection** tracks live selection (mouseup), not only title  
2. **Marketplace** buy→host HTML port polish (catalog exists)  
3. **NotDiamond advisory** weekly refresh UX if usage warrants  
4. **Antiek-bench** sub-benchmark promote UI polish (propose≠promote)  
5. **Midnight Oil** job history + deposit twin seed if thin gaps remain  
6. **Competitive deep-research** backlog aq–av from VERDICT.md  

## Invariants

HTML view_format · budget soft-gate · propose≠promote · NotDiamond advisory only

## Next

**(gg)+** re-derive product residual; operator merge gated.
