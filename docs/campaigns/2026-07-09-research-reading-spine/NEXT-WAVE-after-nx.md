# Next-wave residual map after (nx) · PR #465

## Closed (nw→nx)
KNOWN_USAGE_FEED_SOURCES includes twin_chase + floating_deep_research;
weekly_usage_summary + Settings API pass by_source + known_sources; Settings
UI legend data-has-twin-chase / data-has-floating-dr. Fixed API by_source
passthrough on usage-summary response.

## Next
1. Operator merge PR #465 (highest leverage)
2. SESSION-ARC nw–nx Antiek-bench chase feed handoff
3. Live L1–L4 dual-gate remains operator-only

**(ny)+** re-derive. Infinite continues until STOP.
