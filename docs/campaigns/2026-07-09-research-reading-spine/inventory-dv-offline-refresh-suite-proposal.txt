# residual (dv) refresh suite proposal after offline dogfood
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(du) prior | branch_only | campaign inventories
(dv) offline → proposal refresh | branch_only | Settings onRunOfflineDogfood refreshes suite proposal; vitest

## What shipped
- After offline dogfood, fetch suite-proposal (proposed only) into Settings panel.
- Non-fatal if proposal refresh fails (offline run still success).

NEXT: (dw) re-derive; operator merge PR #465
