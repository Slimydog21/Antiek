# residual (fa) remount twin notes on DR host with context refresh key
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(ez) prior | branch_only | campaign inventories
(fa) DR twins remount | branch_only | DeepResearchSessionHost twins refresh key parity with hosted ez; 22 vitest

PATHS:
- apps/reading/src/components/windows/DeepResearchSessionHost.tsx
- apps/reading/src/components/windows/DeepResearchSessionHost.test.tsx

INVARIANTS:
- Same contextRefreshKey as ResearchContextPanel
- Remount after promote/attach/flywheel/merge/collective

NEXT: (fb) re-derive; operator merge PR #465
