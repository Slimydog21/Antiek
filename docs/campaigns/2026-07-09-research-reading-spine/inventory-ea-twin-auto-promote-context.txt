# residual (ea) auto-promote twins to research context after load
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(dz) prior | branch_only | campaign inventories
(ea) twin autoPromoteAfterLoad | branch_only | TwinNotesPanel autoPromoteAfterLoad; DR + hosted hosts; 26 vitest

## What shipped
- TwinNotesPanel `autoPromoteAfterLoad` promotes notes into context after autoLoad/seed.
- Enabled on DeepResearchSessionHost + HostedHtmlDocumentHost with autoSeedIfEmpty.
- Status line twin-promote-status for honesty.

## Why
Recursive note-taker substrate must inform prompts without a manual promote click.

NEXT: (eb) re-derive; operator merge PR #465
