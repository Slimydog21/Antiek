# residual (fu) multi-section HTML outline import (h1–h3 split)
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(ft) prior | branch_only | campaign inventories
(fu) multi-section HTML import | branch_only | splitHtmlIntoOutlineSections; createWithConnection loops sections; 6 unit + 10 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/htmlDraftImport.ts
- apps/reading/src/modes/Write/htmlDraftImport.test.ts
- apps/reading/src/modes/Write/WriteHome.tsx
- apps/reading/src/modes/Write/WriteHome.test.tsx

INVARIANTS:
- HTML-only; PDF refused
- No headings → single section fallback
- Cap MAX_OUTLINE_SECTIONS=20
- promote_to_graph false

NEXT: (fv) re-derive; operator merge PR #465
