# residual (bn) Midnight Oil offline worker run product path
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(bm) prior | branch_only | campaign inventories
(bn) offline worker run | branch_only | run_job_offline + offline_goal_step_fn; POST /midnight-oil/run; optional auto_deposit; UI "Run offline worker" + moil-run-result; tests 23 pytest + 3 vitest; tip after commit

PRODUCT PATH (honest offline stub until live multi-provider inject):
create → recommend ceiling → approve → run offline (FakeClock + worker loop) → optional deposit HTML twins

HONESTY:
- offline=True; no live model/network calls in default step_fn
- spent_per_goal stub for budget-halt testing
- view_format=html; no PDF

NEXT: (bo) re-derive highest-value residual (collective research merge product polish / NotDiamond advisory surface / Antiek-bench dogfood run / marketplace HTML book host); operator merge PR #465
