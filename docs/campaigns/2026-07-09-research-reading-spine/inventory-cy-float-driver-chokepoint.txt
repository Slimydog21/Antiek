# residual (cy) decision-tree model_id chokepoint in launchFloatingDeepResearch
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(cx) prior | branch_only | campaign inventories
(cy) float launch driver chokepoint | branch_only | launchFloatingDeepResearch resolves decision-tree model_id when omitted; float-menu/ResearchThis/HighlightToolbar share path; 5+4 vitest

## What shipped
- `resolveDecisionTreeModelId()` + auto-resolve inside `launchFloatingDeepResearch` when model_id empty.
- Explicit non-empty model_id wins (no tree fetch).
- Driver fetch failure non-fatal → model_id null; session still opens.
- Result exposes resolved `model_id` for analytics.
- ResearchThis no longer duplicates fetch (budget panel remains cx).

## Why (north star)
Reading highlight float-menu and ResearchThis both fire floating deep research; model choice must live at one composition boundary so every path uses the Settings decision-tree driver without inventing models.

NEXT: (cz) re-derive; operator merge PR #465
