# residual (fx) HTML-first section prose from html_fragment
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(fw) prior | branch_only | campaign inventories
(fx) HTML fragment prose | branch_only | OutlineSectionImport.html_fragment; updateSectionProse prefers HTML; 8 unit + 10 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/htmlDraftImport.ts
- apps/reading/src/modes/Write/WriteHome.tsx

INVARIANTS:
- HTML-first prose when fragment present; plain fallback
- No PDF path
- Scripts/styles stripped before fragment cut

NEXT: (fy) re-derive; operator merge PR #465
