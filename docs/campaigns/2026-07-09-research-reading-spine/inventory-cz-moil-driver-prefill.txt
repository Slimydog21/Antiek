# residual (cz) Midnight Oil decision-tree model prefill
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(cy) prior | branch_only | campaign inventories
(cz) moil driver prefill | branch_only | MidnightOil prefill model_id from decision tree; DecisionTreeDriverBadge; 6 vitest

## What shipped
- On mount: fetchDecisionTreeSelection → if installed, set model id input.
- DecisionTreeDriverBadge + prefill status (installed/none/error/pending).
- Model field remains editable override.
- When no driver: keep "default" for ceiling pricing compatibility.

## Why (north star)
Midnight Oil autonomous runs should inherit the operator's Settings decision-tree driver without re-typing model ids — same driver authority as workstation launches.

NEXT: (da) re-derive; operator merge PR #465
