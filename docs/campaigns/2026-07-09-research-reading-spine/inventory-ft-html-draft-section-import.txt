# residual (ft) HTML draft plain text → Write outline section 0 on create
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(fs) prior | branch_only | campaign inventories
(ft) HTML draft section import | branch_only | createWithConnection → createSection + updateSectionProse; import-on-create badge; 9 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/WriteHome.tsx
- apps/reading/src/modes/Write/WriteHome.test.tsx
- apps/reading/src/modes/Write/htmlDraftImport.ts

INVARIANTS:
- HTML-only draft source (prepareHtmlDraftForWrite)
- Plain text into section prose (not PDF)
- Import failure non-fatal (piece still opens)
- promote_to_graph false

NEXT: (fu) re-derive; operator merge PR #465
