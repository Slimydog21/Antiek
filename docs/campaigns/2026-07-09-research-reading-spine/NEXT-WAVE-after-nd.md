# Next-wave residual map after (nd) · PR #465

## Closed (nc→nd)
One-click Select questions / Select insights into multi-select set
(union with existing selection). Enables chase/promote questions path
without manual checkbox grind.

## Next
1. Operator merge PR #465 (highest leverage)
2. Invert multi-select / replace-vs-union mode
3. Collective chase-spawn multi-select polish
4. Live L1–L4 dual-gate remains operator-only

**(ne)+** re-derive. Infinite continues until STOP.
