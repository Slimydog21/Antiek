# Session arc · Midnight Oil budget fit + catalog HTML honesty · (md)–(mf) · PR #465

**Tip:** re-read `git log -1` on campaign branch.  
**Prior arc:** `SESSION-ARC-lw-mb-marketplace-catalog.md` (catalog product-complete).  
**STOP:** operator only.

## Residuals closed

| Residual | Product delta |
|---|---|
| **md** | MO recommended ceiling vs remaining daily budget fit (fits\|may_exceed\|unknown) |
| **me** | Soft-gate approve when may_exceed; force override; unknown never blocks |
| **mf** | Catalog HTML projection By source / By subject honesty lines |

## Operator path (Midnight Oil)

1. Goals + duration + depth tier → create recommends ceiling  
2. See budget fit chrome (remaining from Settings projection)  
3. Approve at recommended / custom — blocked if may_exceed unless force  
4. Run → deposit → twins / Write / progress  

## Invariants

- Never invent $0 remaining budget  
- Explicit approve before swarm work  
- Soft gates only when remaining is known  
- Catalog HTML stays offline-honest (no payment rails)

## Next

**(mg)+** re-derive. Dual-gate L1–L4 dogfood, competitive DR polish, or operator
merge PR #465. Infinite continues until STOP.
