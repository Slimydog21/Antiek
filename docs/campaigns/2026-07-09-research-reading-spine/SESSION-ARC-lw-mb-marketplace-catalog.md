# Session arc · marketplace catalog product-complete · residuals (lw)–(mb) · PR #465

**Tip:** re-read `git log -1` on `campaign/research-reading-spine-2026-07-09-main`.  
**Plan AC:** suite-proposal `4_passed`.  
**STOP:** operator only (`.infinite/STOP` or explicit message).  
**Worktree:** `~/Antiek/platform/worktrees/campaign-rrs-main`

## Product arc (HTML-first knowledge marketplace)

Reading ≡ research for hosted books. Offline demo catalog; no live payment rails;
no live external connectors (L1/L2 dual-gate).

| Residual | Product delta |
|---|---|
| **lw** | `CatalogEntry.subjects` + STEM PD spine (Euclid/Newton/Bacon) + `by_subject` honesty + domain chips |
| **lx** | `Catalog.filter_by_source` + knowledge-source chips (compose free-PD × subject × text) |
| **ly** | `project_catalog_html` + GET `/marketplace/catalog` html + Open catalog as HTML window |
| **lz** | PublicationAttachPanel DecisionTreeDriverBadge + session `researchTier` wire |
| **ma** | Host/purchase → Antiek-bench `book_qa` usage (`source=marketplace_host`) |
| **mb** | Host-land usage_event UI chrome (propose≠promote honesty) |

## Operator path

1. Filter free PD / subject domain / knowledge source  
2. Open catalog as HTML (optional)  
3. Host HTML book → twin seed → DR float|full or Write draft  
4. Host records book_qa usage → Settings suite proposal can learn (operator still promotes)

## Filter matrix (offline demo — product-complete)

`free_pd_only × subject_chip × source_chip × text_query × HTML_open`

## Still deferred

| ID | Gap |
|---|---|
| L1 | Live arxiv body hydrate |
| L2 | Live Substack body |
| L5 | Live payment rails |
| — | Live external catalog connectors |

## Invariants (hard to vary)

- Human view = HTML; PDF ingest only  
- Soft budget; NotDiamond advisory only  
- propose≠promote for Antiek-bench  
- Offline-honest hydrate/twin/MO by default  
- Main merge / prod = operator only (PR #465)

## Next

**(md)+** re-derive from north star (competitive DR polish, dual-gate dogfood prep,
Midnight Oil UX, recursive note-taker depth). Infinite continues until STOP.
