# Next-wave residual map after (fa) · PR #465

Campaign tip at ship of this note: see `cycle-state.json` / git tip on
`campaign/research-reading-spine-2026-07-09-main`. This is an **honest gap map**
for future agents — not a claim that the north star is finished.

## Closed on branch (session el→fa, 2026-07-09)

| Id | Capability |
|----|------------|
| el | draft_combined spawn merge auto-opens hosted HTML |
| em | collective draft + written analysis auto-open (shared chokepoint) |
| en | hosted book highlight → float DR + budget |
| eo | collective document merge twin seed |
| ep | collective onDocMerged remounts research context |
| eq | DecisionTreeDriverBadge compact budget usage bar |
| er | hosted book optional arxiv/substack pub refs |
| es | hosted DR floating \| full |
| et | ResearchThis deep research full window (≠ legacy /inv) |
| eu | hosted book CollectiveResearchPanel over open DR spawns |
| ev | merge/analysis open full window |
| ew | Midnight Oil deposit open full |
| ex | Midnight Oil auto-open deposit after deposit/auto-deposit |
| ey | collective continue-as-unit full |
| ez | hosted twins remount with context refresh |
| fa | DR host twins remount with context refresh |
| fb | next-wave honest gap map |
| fc | Reading ResearchThis collective multi-select |

## Operator / env gated (do not fake green)

1. **PR #465 main merge** — operator only.
2. **Live multi-agent collective chat council** — see `spec-dn-collective-live-multiagent.md`.
3. **Live Midnight Oil multi-provider workers** — see `MIDNIGHT-OIL-LIVE-STEP.md`.
4. **Live hydrate / twin seed injectors** — see `TWIN-SEED-LIVE.md`.
5. **Paid marketplace rails** — catalog/host HTML path is offline-honest; payments gated.
6. **NotDiamond dispatch authority** — advisory only; install as decision-tree driver, never sole router.
7. **Antiek-bench weekly LaunchAgent install** — script + plist example ship; operator installs.
8. **Live arxiv/substack network hydrate** — offline-honest adapters + HTML-first stance.

## Highest-value unblocked product residuals (re-derive each cycle)

Prefer thin chokepoint product over parallel UIs:

1. **Reading main surface collective** — mount collective multi-select when reading page has open DR windows (parity eu for non-marketplace books).
2. **Twin recursive depth** — twin-of-twin promote chain metrics in ResearchContextPanel.
3. **Antiek-bench task taxonomy rewrite UI** — display proposed sub-benchmarks with explicit promote gate.
4. **Model decision-tree tab** — Settings deep-link from driver badge + per-key usage if multi-key.
5. **HighlightToolbar / float-menu full mode** — parity et/es if not already wired.
6. **Citation trust graph viz** — evidence pack already honest; visual edge browser optional.
7. **Writing mode** — merge draft_combined HTML into Write mode surface (HTML-first).

## Invariants that must not regress

- Human-viewable assets: **HTML `view_format` only** (PDF never required as view).
- Budget soft-gates remain soft (force override); never silent fire over budget.
- Bench suite **propose ≠ auto-promote**.
- NotDiamond **advisory only**.
- Live injectors **env-gated** with documented steps.

## Next residual letter

**(fd)** — re-derive; tip advanced past fc.
Operator merge still gated.
