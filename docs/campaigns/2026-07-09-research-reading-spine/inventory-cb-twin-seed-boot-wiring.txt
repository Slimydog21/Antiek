# residual (cb) Twin seed live boot wiring
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(ca) prior | branch_only | campaign inventories
(cb) twin seed boot wiring | branch_only | twin_seed_live_wiring.configure_twin_seed_from_env; app.py call; dual env gate; 3 pytest; tip after commit

ENV: ANTIEK_TWIN_SEED_LIVE + ANTIEK_TWIN_SEED_USE_DISPATCH (both required for dispatch inject)

NEXT: (cc) re-derive; operator merge PR #465
