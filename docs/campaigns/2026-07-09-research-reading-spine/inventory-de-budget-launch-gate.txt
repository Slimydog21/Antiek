# residual (de) budget projection callback + ResearchThis soft gate
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(dd) prior | branch_only | campaign inventories
(de) budget launch gate | branch_only | ResearchLaunchBudgetPanel onProjectionChange; ResearchThis soft-disable + force override; vitest

## What shipped
- `onProjectionChange` exposes wouldExceedBudget / pricing / model to parents.
- ResearchThis disables float open when would_exceed unless force override checked.
- Honest: unknown projection does not block (only true would_exceed).

## Why (north star)
Operator sees budget impact *and* cannot accidentally fire a prompt that the projection says will blow the daily cap — with explicit force path for judgment calls.

## Next-wave gaps (operator / env-gated — not silently faked)
1. Live Midnight Oil multi-provider worker (ANTIEK_MIDNIGHT_OIL_LIVE_STEP)
2. Live arxiv/substack hydrate injectors
3. Live twin seed LLM note_taker (env dual-gate)
4. Marketplace live payment rails (purchase UI is receipt-based offline)
5. Antiek-bench weekly LaunchAgent / cron (operator machine)
6. NotDiamond Wave-1 live adapter (kill-switch; advisory install remains)
7. Collective live multi-agent chat (merge+continue unit ships; shared live session does not)

NEXT: (df) re-derive; operator merge PR #465
