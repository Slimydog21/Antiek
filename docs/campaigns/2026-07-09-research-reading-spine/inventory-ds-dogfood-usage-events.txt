# residual (ds) offline dogfood records usage events for suite rewrite
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(dr) prior | branch_only | campaign inventories
(ds) dogfood → usage events | branch_only | record_dogfood_runs_as_usage in product_path; 3 pytest; weekly smoke has_proposal

## What shipped
- `record_dogfood_runs_as_usage` maps item scores → UsageEvent (worked/failed).
- `run_offline_dogfood_product(..., record_usage_events=True)` default on.
- usage_events_recorded on product payload; never auto-promotes suite.
- Closes recursive offline flywheel: dogfood → usage → suite-proposal (proposed).

NEXT: (dt) re-derive; operator merge PR #465
