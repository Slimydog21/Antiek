# Next-wave residual map after (om) · PR #465

## Closed (ol→om)
WriteHome CollectiveResearchPanel now includes session recent_ring spawn ids
(listRecentDeepResearchSpawnIds + recentTick clear path), parity with DR host,
hosted HTML host, and ResearchThis. Twin-chase floats that close still appear
for multi-select merge into the open Write piece.

## Next
1. Operator merge PR #465 (highest leverage)
2. Dogfood residual: last surface without recent_ring — inventory complete for
   engagement hosts; pivot to product gaps (Midnight Oil offline honesty,
   marketplace HTML port polish, Antiek-bench recursive rewrite UI, pub-ref
   connectors depth, decision-tree projection fidelity)
3. Live L1–L4 dual-gate remains operator-only
4. Spec-first when product/tech gaps need precision for future agents

**(on)+** re-derive. Infinite continues until STOP.
