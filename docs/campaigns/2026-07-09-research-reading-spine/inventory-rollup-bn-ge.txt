# Campaign rollup (bn)–(ge) · PR #465
Write piece deep research + pub refs (ge); open-piece HTML re-import (gd);
driver/context/twins (gc–ga). Reading≡write research launch parity advanced.
Operator merge still gated. Next residual (gf).
