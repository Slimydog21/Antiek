# Session arc · collective Antiek-bench feed · (oi)–(oj) · PR #465

**Tip:** re-read `git log -1` on campaign branch.  
**Prior:** SESSION-ARC-of-og · inventory-oi · inventory-oj  
**STOP:** operator only. Infinite continues until STOP.

## Product closed

| Residual | Delta |
|---|---|
| **oi** | `record_collective_merge_usage` on POST `/collective` + `/merge` |
| **oj** | Collective unit + doc-merge UI surfaces `usage_event` metrics |

## Twin chase → collective → bench rewrite path

```
twin multi-select → chase (twin_chase usage)
  → recent_ring + collective multi-select
  → Select recent / merge prompt / draft_combined / written analysis
  → collective_merge usage_event (task_class from recommended tier)
  → weekly_usage_summary.by_source + known_sources
  → suite proposal rewrite (propose≠promote)
```

## Known feed sources (partial)

investigation_start · session_flywheel · midnight_oil · marketplace_host ·
floating_deep_research · twin_chase · **collective_merge** · offline_dogfood

## Proof

- pytest `test_collective_merge_usage_oi` + engagement collective  
- vitest CollectiveResearchPanel 15 (usage metrics)

## Next

1. **Operator merge PR #465**
2. Continue dogfood product polish
3. Live L1–L4 dual-gate only

**(ok)** this arc. Infinite continues until STOP.
