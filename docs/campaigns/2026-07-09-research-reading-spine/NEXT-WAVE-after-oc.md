# Next-wave residual map after (oc) · PR #465

## Closed (ob→oc)
Collective "Clear recent" drops session recent-spawn ring; hosts re-tick
availableSpawnIds via onRecentSpawnsCleared.

## Next
1. Operator merge PR #465 (highest leverage)
2. SESSION-ARC ob–oc collective recent spawns handoff
3. Live L1–L4 dual-gate remains operator-only

**(od)+** re-derive. Infinite continues until STOP.
