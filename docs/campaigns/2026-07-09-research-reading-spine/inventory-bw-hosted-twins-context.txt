# residual (bw) Hosted book window: twins + research context flywheel
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(bv) prior | branch_only | campaign inventories
(bw) hosted twins+context mounts | branch_only | HostedHtmlDocumentHost TwinNotesPanel + ResearchContextPanel; 3 vitest; tip after commit

Closes reading↔research loop on hosted HTML books (document_id = engagement asset_id).

NEXT: (bx) re-derive; operator merge PR #465
