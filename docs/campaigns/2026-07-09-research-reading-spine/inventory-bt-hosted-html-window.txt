# residual (bt) Hosted HTML book floating window
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(bs) prior | branch_only | campaign inventories
(bt) hosted_html_document window | branch_only | HostedHtmlDocumentHost; openWindow kind; MarketplaceHost Open hosted book in window; 5 vitest; tip after commit

HTML-FIRST: view_format must be html; PDF rejected as reading surface

NEXT: (bu) re-derive; operator merge PR #465
