# residual (gl) Midnight Oil deposit ResearchProgressPanel
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(gk) prior | branch_only | campaign inventories
(gl) moil progress mount | branch_only | moil-deposit-progress-mount + ResearchProgressPanel per spawn; 6 MidnightOil vitest

PATHS:
- apps/reading/src/modes/MidnightOil/index.tsx
- apps/reading/src/modes/MidnightOil/MidnightOil.test.tsx

BEHAVIOR:
- When deposit.spawn_ids non-empty, mount ResearchProgressPanel per spawn
- autoLoad + autoSeedIfEmpty (competitive multi-minute telemetry)
- Shared panel with DR host (no fork)

NEXT: (gm) re-derive; operator merge PR #465
