# residual (ge) deep research + pub refs on open Write piece
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(gd) prior | branch_only | campaign inventories
(ge) write piece DR launch | branch_only | write-piece-research-launch + pub refs + budget + float|full; 15 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/WriteHome.tsx
- apps/reading/src/modes/Write/WriteHome.test.tsx

BEHAVIOR:
- Open piece mounts research launch panel (data-view-format=html)
- Optional arxiv/substack/URL pub refs → hydratePublicationRefs + references on launch
- ResearchLaunchBudgetPanel soft-gate + force-over-budget
- launchFloatingDeepResearch float | full via shared chokepoint
- Highlight selection wins; else title-based selection
- Context refresh after successful launch

INVARIANTS:
- HTML view_format
- Budget soft-gate (never hard-block without override path)
- NotDiamond advisory only
- Uses shipped launchFloatingDeepResearch (no parallel spawn path)

NEXT: (gf) re-derive; operator merge PR #465
