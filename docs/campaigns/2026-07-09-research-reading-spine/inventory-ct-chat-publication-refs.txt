# residual (ct) Publication refs on ChatInputArea (chase/composer parity)
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(cs) prior | branch_only | campaign inventories
(ct) chat publication refs | branch_only | ChatInputArea pub refs + hydrate + ground question; 3 vitest; tip after commit

PRODUCT PATH: chase Ask with arxiv/substack lines → hydrate HTML assets → ground prompt

NEXT: (cu) re-derive; operator merge PR #465
