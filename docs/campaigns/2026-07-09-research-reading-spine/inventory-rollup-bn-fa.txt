# Campaign rollup (bn)–(fa) · PR #465
DR + hosted twins remount with context refresh; full/float DR/merge/moil; draft auto-open; driver budget; hosted collective.
Operator merge still gated. Next residual (fb).
