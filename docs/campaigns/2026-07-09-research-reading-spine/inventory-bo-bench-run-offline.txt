# residual (bo) Antiek-bench offline dogfood suite run product path
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(bn) prior | branch_only | campaign inventories
(bo) offline dogfood run | branch_only | run_offline_dogfood_product; POST /settings/antiek-bench/run-offline; Settings "Run offline dogfood suite"; populates leaderboard; 3 pytest + Settings vitest; tip after commit

PRODUCT PATH:
fixtures (av) → offline run (bo) → weekly leaderboard (al) → install decision-tree (bl) → usage rewrite propose/approve (am)

HONESTY:
- keyword_stub_provider quality tiers only; offline=True
- never auto-promote suite; never auto-route dispatch
- HTML view only

NEXT: (bp) re-derive (research prompt budget projection at launch / Midnight Oil live injectors env-gated / collective engage unit polish); operator merge PR #465
