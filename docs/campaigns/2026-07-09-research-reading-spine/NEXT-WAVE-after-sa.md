# Next-wave residual map after (sa) · PR #465

## Closed
- Write-seed honesty rt–rz
- **sa:** decision-tree embeds budget usage bar + project-cost deep-link

## Next
1. Operator merge PR #465 (highest leverage — gated)
2. **sb candidate:** decision-tree panel inline mini prompt projection
   (sample chars → would_exceed_budget) so operators see impact before install
   without scrolling to full Prompt cost projection card
3. Live L1–L4 dual-gate remains operator-only

**(sb)+** re-derive. Infinite continues until STOP.
