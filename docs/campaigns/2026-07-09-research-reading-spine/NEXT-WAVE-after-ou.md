# Next-wave residual map after (ou) · PR #465

## Closed (ot→ou)
ResearchThis collective mount exposes `data-recent-count` and vitest proves
recent_ring is passed into collectDeepResearchSpawnIds + CollectiveResearchPanel
(parity WriteHome om). Reading surface closed-window multi-select is
mechanically proven.

## Next
1. Operator merge PR #465 (highest leverage)
2. HostedHtmlDocumentHost recent_ring data-recent-count + test parity (ov)
3. Live L1–L4 dual-gate remains operator-only

**(ov)+** re-derive. Infinite continues until STOP.
