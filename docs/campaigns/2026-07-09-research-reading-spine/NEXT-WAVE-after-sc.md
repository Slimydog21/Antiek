# Next-wave residual map after (sc) · PR #465

## Closed (driver foresight loop sa–sc)
- sa budget bar · sb mini projection · sc launch → decision-tree deep-link

## Next
1. Operator merge PR #465 (highest leverage — gated)
2. **sd candidate:** other Settings deep-links that still point bare /settings
   (DecisionTreeDriverBadge, MO, etc.) → #decision-tree-panel where budget/driver
   is the intent
3. Live L1–L4 dual-gate remains operator-only

**(sd)+** re-derive. Infinite continues until STOP.
