# residual (df) workstation Ask soft-gate on budget would_exceed
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(de) prior | branch_only | campaign inventories
(df) workstation budget gate | branch_only | ChatInputArea + StartResearch soft-gate Ask; 18 vitest

## What shipped
- ChatInputArea + StartResearch wire onProjectionChange; disable Ask (and Break-into-sub-questions) when would_exceed unless force override.
- Parity with ResearchThis (de). Unknown projection never blocks.

NEXT: (dg) re-derive; operator merge PR #465
