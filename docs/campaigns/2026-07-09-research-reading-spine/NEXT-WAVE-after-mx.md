# Next-wave residual map after (mx) · PR #465

## Closed (mw→mx)
Multi-select twin `note_id` promote: substrate + API accept `note_ids`;
TwinNotesPanel checkboxes + Select visible / Clear / Promote selected;
intersection with kinds filter when both set (mq + mx).

## Next
1. Operator merge PR #465 (highest leverage — not agent-owned)
2. Twin multi-select polish (select-all visible already partial; clear after promote)
3. Competitive DR polish (citation trust, long-horizon wrestle when live step)
4. Live L1–L4 dual-gate remains operator-only

**(my)+** re-derive. Infinite continues until STOP.
