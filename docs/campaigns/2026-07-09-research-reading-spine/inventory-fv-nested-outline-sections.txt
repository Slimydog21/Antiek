# residual (fv) nest HTML outline sections by heading level
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(fu) prior | branch_only | campaign inventories
(fv) nested sections | branch_only | heading_level on OutlineSectionImport; parent_section_id on createSection for h2/h3; 7 unit + 10 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/htmlDraftImport.ts
- apps/reading/src/modes/Write/WriteHome.tsx
- apps/reading/src/modes/Write/WriteHome.test.tsx

INVARIANTS:
- h1 top-level; h2/h3 parent walks up to nearest shallower level
- HTML-only import path
- Cap MAX_OUTLINE_SECTIONS=20

NEXT: (fw) re-derive; operator merge PR #465
