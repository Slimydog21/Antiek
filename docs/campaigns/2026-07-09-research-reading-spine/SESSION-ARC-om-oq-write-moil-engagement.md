# Session arc · Write + Midnight Oil engagement parity · (om)–(oq) · PR #465

**Tip:** `a55ef629` on `campaign/research-reading-spine-2026-07-09-main`.  
**Prior:** SESSION-ARC-oi-oj · inventory-om…inventory-oq  
**STOP:** operator only. Infinite continues until STOP.

## Product closed

| Residual | Delta |
|---|---|
| **om** | WriteHome collective wires `recent_ring` + `onRecentSpawnsCleared` (parity DR/hosted/ResearchThis) |
| **on** | Midnight Oil deposit mounts CollectiveResearchPanel; deposit spawn_ids → recent_ring |
| **oo** | TwinNotesPanel on MO deposit (autoLoad/seed/promote + remount key) |
| **op** | ResearchContextPanel on MO deposit (same remount key as twins) |
| **oq** | Offline run `spawn_ids` → recent_ring even when `auto_deposit` is off (`rememberSpawnIds`) |

## Operator vision path closed offline

```
Midnight Oil: goals + duration + ceiling approve
  → offline run (spawn_ids) → recent_ring always
  → optional deposit HTML asset
  → twins · research context · progress · collective multi-select
  → merge/analysis into deposit parent
  → Write / hosted / ResearchThis collective also see recent_ring
```

```
Write open piece:
  → twin-chase closed floats still multi-select via recent_ring (om)
  → merge into writing asset parent
```

## Engagement spine surfaces (parity)

| Surface | twins | context | collective + recent_ring |
|---|---|---|---|
| DeepResearchSessionHost | yes | yes | yes |
| HostedHtmlDocumentHost | yes | yes | yes |
| ResearchThis | — | — | yes |
| WriteHome open piece | yes | yes | yes (om) |
| Midnight Oil deposit | yes (oo) | yes (op) | yes (on) + run ring (oq) |

## Invariants preserved

- HTML-first human view  
- Soft budget · never invent $0  
- Offline-honest MO by default  
- NotDiamond advisory only (L7)  
- propose≠promote Antiek-bench  

## Proof

- vitest WriteHome 21 (om)  
- vitest MidnightOil 15 (on–oq)  
- tip `a55ef629`

## Next

1. **Operator merge PR #465** (highest leverage)
2. Antiek-bench recursive rewrite / marketplace / pub-ref depth polish
3. Live L1–L4 dual-gate only

**(or)** this arc. Infinite continues until STOP.
