# Next-wave residual map after (ot) · PR #465

## Closed (os→ot)
Midnight Oil run metrics surface `data-recent-ring-count` +
`data-recent-ring-has-run-spawns` and honesty copy that offline swarm spawn ids
are remembered for collective multi-select (Write/hosted/ResearchThis/deposit)
even when auto-deposit is off.

## Next
1. Operator merge PR #465 (highest leverage)
2. Spec-first / product residual on remaining vision gaps
3. Live L1–L4 dual-gate remains operator-only

**(ou)+** re-derive. Infinite continues until STOP.
