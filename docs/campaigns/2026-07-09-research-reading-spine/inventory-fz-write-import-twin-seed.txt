# residual (fz) offline twin seed after Write HTML draft import
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(fy) prior | branch_only | campaign inventories
(fz) write import twin seed | branch_only | seedTwinNotes force_offline on deliverable_id after section import; 10 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/WriteHome.tsx
- apps/reading/src/modes/Write/WriteHome.test.tsx

INVARIANTS:
- force_offline honest seed
- Non-fatal if twin seed fails
- HTML-first draft path only

NEXT: (ga) re-derive; operator merge PR #465
