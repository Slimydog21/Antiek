# residual (ev) open merged draft HTML full working-region window
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(eu) prior | branch_only | campaign inventories
(ev) merge open full | branch_only | openMergedResearchWindow windowMode full; spawn + collective buttons; 5 spawn + 8 collective vitest

PATHS:
- apps/reading/src/components/engagement/SpawnMergePanel.tsx
- apps/reading/src/components/engagement/CollectiveResearchPanel.tsx

INVARIANTS:
- Auto-open remains floating (el/em)
- Full re-open uses distinct window id suffix :full
- HTML-only open

NEXT: (ew) re-derive; operator merge PR #465
