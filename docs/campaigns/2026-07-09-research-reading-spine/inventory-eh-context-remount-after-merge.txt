# residual (eh) remount research context after spawn merge
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(eg) prior | branch_only | campaign inventories
(eh) context remount after merge | branch_only | SpawnMergePanel onMerged; DR host remount; 24 vitest

## What shipped
- SpawnMergePanel `onMerged` after HTML merge + twin seed attempt.
- DeepResearchSessionHost wires onMerged → contextRefreshKey.

NEXT: (ei) re-derive; operator merge PR #465
