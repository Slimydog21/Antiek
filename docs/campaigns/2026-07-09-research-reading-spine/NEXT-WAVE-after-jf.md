# Next-wave residual map after (jf) · PR #465

## Closed (je→jf)
CollectiveResearchPanel continue-as-unit budget prefills researchTier from Settings depth-tier
(fetchDepthTiers → mapDepthTierToResearchTier; data-depth-prefill + allowTierPick).

## Highest-value unblocked next
1. ResearchThis.tsx Settings depth prefill (still hardcoded researchTier="deep")
2. WriteHome.tsx piece DR budget depth prefill (still hardcoded researchTier="deep")
3. DEFERRED-GAPS L1–L4 live injectors (operator dual-gate)
4. Operator: merge PR #465

## Next
**(jg)+** ResearchThis depth prefill parity; operator merge gated.
