# residual (dn) collective live multi-agent deferred spec
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(dm) prior | branch_only | campaign inventories
(dn) live council spec | branch_only | spec-dn-collective-live-multiagent.md — deferred code until operator unlock

## What shipped
- Deferred product spec: live multi-agent council vs shipped merge+continue unit.
- Rejects fake single-model council; hard requirements for budget/HTML/twins/env gates.

NEXT: (do) re-derive product residual; operator merge PR #465
