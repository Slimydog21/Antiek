# Campaign rollup (bn)–(gg) · PR #465
Write twins remount (gg); collective (gf); DR+pubs (ge); re-import (gd).
Write≡hosted engagement flywheel closed for core surfaces.
Operator merge still gated. Next residual (gh).
