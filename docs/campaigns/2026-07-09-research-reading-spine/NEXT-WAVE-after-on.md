# Next-wave residual map after (on) · PR #465

## Closed (om→on)
Midnight Oil deposit mounts CollectiveResearchPanel over deposit spawn_ids +
open windows + recent_ring; pushes deposit spawns into session recent_ring so
offline swarm goals remain multi-selectable into the deposit HTML asset
without leaving the MO surface.

## Next
1. Operator merge PR #465 (highest leverage)
2. TwinNotesPanel on MO deposit (parity gk reseed UI) if dogfood shows gap
3. Marketplace catalog → hosted book → collective recent_ring already on host
4. Antiek-bench recursive rewrite weekly UI polish / usage projection fidelity
5. Live L1–L4 dual-gate remains operator-only
6. Spec-first for product/tech gaps that need precision for future agents

**(oo)+** re-derive. Infinite continues until STOP.
