# inventory-qw · ResearchProgress terminal → Open Write twin_seed
branch: campaign/research-reading-spine-2026-07-09-main
residual: qw
date: 2026-07-10

## Closed
- TwinWriteSeedSource research_progress_complete (store + load)
- buildResearchProgressWriteHref — only when isTerminal; twin_seed only
  - plain = goal + spawn + stage + tier + event lines + html strip
  - HTML article data-source=research_progress_complete data-is-terminal=true
  - asset_id = parentAssetId || deep_research:{spawn}
- ResearchProgressPanel Open Write (research complete) when terminal
  - data-testid=research-progress-open-write
  - optional parentAssetId + goal props for provenance
- DeepResearchSessionHost wires parent_asset_id + goal into progress panel
- vitest twinWriteSeed 13 + ResearchProgressPanel 9 = 22 passed
  (SCRATCH residual-tests-qw.log)

## Recursive note-taker loop
highlight → float DR (qv selection seed) → multi-minute progress → terminal (qw)
→ Write brainstorm with twin_seed → create seeds twins (pq)
