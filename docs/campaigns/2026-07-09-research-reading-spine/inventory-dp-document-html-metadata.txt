# residual (dp) hosted document HTML response includes title/license
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(do) prior | branch_only | campaign inventories
(dp) document html metadata | branch_only | GET /marketplace/documents/{id}/html returns title, license_class, source; 3 pytest

## What shipped
- Document HTML route returns title, license_class, owner_id, source for rehydrate UX.
- RuntimeError (PDF/empty) → 400 not 500.
- Routes test asserts metadata identity.

NEXT: (dq) re-derive; operator merge PR #465
