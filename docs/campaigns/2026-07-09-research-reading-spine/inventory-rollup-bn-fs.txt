# Campaign rollup (bn)–(fs) · PR #465
Next-wave map after fr (fs). Write HTML path + launch budget Settings + merge/moil Write handoffs closed on branch.
Operator merge still gated. Next residual (ft).
