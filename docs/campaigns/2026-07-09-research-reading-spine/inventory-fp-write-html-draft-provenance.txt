# residual (fp) Write HTML draft provenance freeform stamp
# PR #465 · 2026-07-09

CAPABILITY | STATUS | EVIDENCE
(a)–(fo) prior | branch_only | campaign inventories
(fp) write draft provenance | branch_only | projectType freeform html_draft:document_id on load; write-html-draft-provenance; 8 WriteHome vitest

PATHS:
- apps/reading/src/modes/Write/WriteHome.tsx
- apps/reading/src/modes/Write/WriteHome.test.tsx

INVARIANTS:
- Provenance is operator-visible; not invented body
- Full outline import still deferred

NEXT: (fq) re-derive; operator merge PR #465
