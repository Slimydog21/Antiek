"""Antiek-bench pure substrates (ask #11 — recursive weekly self-rewriting benchmark).

The week-over-week diff (:mod:`week_diff`) compares two weekly snapshots to
surface improvements, regressions, new models, and dropped models — the
"understand what worked and what didn't in a given week" signal that feeds the
recursive rewrite loops.
"""
