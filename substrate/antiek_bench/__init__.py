"""Antiek-bench pure substrates (ask #11 — recursive weekly self-rewriting benchmark).

The snapshot assembler (:mod:`snapshot_assembly`) is the pure glue that turns the
recorder's per-run view records into the :class:`WeeklyBenchSnapshot` the weekly
renderer presents — closing the gap between "recorded runs" and "the rendered report."
"""
