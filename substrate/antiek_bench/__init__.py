"""Antiek-bench pure substrates (ask #11 — recursive weekly self-rewriting benchmark).

The weekly HTML render (:mod:`weekly_render`) turns a week's scored bench runs
into the self-contained HTML the operator sees in Settings — "what models are
best at what tasks" on a weekly basis. Mirrors the plan-tree renderer's
HTML-native, no-JS, all-escaped discipline.
"""
