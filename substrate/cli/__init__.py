"""Antiek substrate CLI surfaces.

Each module here is a self-contained subcommand entrypoint, importable as
``substrate.cli.<name>.main``. Tests invoke them directly with ``main(argv)``
to avoid shell process overhead.
"""
