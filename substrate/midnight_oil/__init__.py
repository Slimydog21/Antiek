"""Midnight Oil pure substrates (ask #13 — autonomous unattended research swarm).

The run receipt (:mod:`run_receipt`) is the honest delivery surface for an
unattended MO run — what the operator sees when they return: did the swarm honor
the approved ceiling, what it produced, and where it stopped.
"""
